# SESSION 217 EXECUTION REPORT — Fable-first: the contrast twin ships, the hazard closes, and the new pin catches its own author twice

Fable-first mode per the standing mandate: the coordinator implemented everything directly; the
only agents spent were one fresh Fable QA (independence is the point) and zero Opus/Sonnet calls.
Credits untouched. Stop-rule honoured after one batch.

## 1. What shipped

**`tse-04-01/i1b` — the no-flip contrast twin (QA: ACCEPT, mathematics 10/10, overall 9.25).**
Nominated by S216's own QA: `3x > 12 → x > 4`, the positive-coefficient case where the ray does
NOT move — the control experiment isolating the negative factor as the cause of reversal. Five
gates adjudicated by the coordinator with REACH verified in widget source; the reachable wrong
action (flipping when you shouldn't) grades incorrect with the direction diagnosis quoting the
learner's own set. QA machine-checked the step's thesis on every edge — **no transform press ever
changes the drawn set** — and confirmed exactly one reachable correct state. The paired
experience now reads: positive divide, nothing moves (04-01) → negative divide, the ray swings
(04-02). Hash 9cf879c3… → 0e4d5281…, revert-proof reproduces the seal byte-for-byte.

**The S216-QA reachability hazard is closed.** With `requireSolvedForm`, coefficient 1 must now
be provably reachable from the start coefficient through the offered transform factors (bounded
BFS over reduced exact rationals) — an author can no longer ship an unsolvable item. Both shipped
i1b lessons pass through the real lint path; mutation-verified (forced-true → 2 red).

**The classic-spec pin now covers the SVG accessible name** (the S216 seal disclosure: the
guarantee was narrower than its claim), derived by an independent transcription of the S215
reading rule, delimiter-anchored.

## 2. Quality control did its job on the coordinator — twice, plus once by QA

Worth stating plainly since the implementor and planner were the same party this session:

- **The new pin caught its own author on first run**: the transcription missed the flat-line case
  (the model prints `y = 4`; the transcription said `y = 0x + 4`). The widget was right; the
  test's independent route was fixed — which is the dual-route design working exactly as built.
- **QA's REQUIRED_FIX 1 caught the pin being weaker than its comment claimed**: inclusion instead
  of anchored matching, so `"y = x + 0".includes("y = x")` would have passed — silently missing
  the dropped-term regression class the pin was built after. Now delimiter-anchored.
- **QA's REQUIRED_FIX 2 caught the guard's message being false on bound-halt**: a schema-legal
  deep ladder halted the bounded search, and the message claimed proved unreachability. The
  refusal now distinguishes "no finite sequence reaches it" (frontier exhausted — proved) from
  "could not confirm within bounds" (halted — refuse-to-certify, worded as what it is). The ×3
  divergence test was re-worded to expect the honest message, with the distinction documented.
- **QA's optional F5 was taken**: the success string said "the line never moved", false of a
  reachable flip-detour route; replaced with the twin's route-true phrasing ("finished exactly
  where it started").
- **Two coordinator claims were corrected by QA on the record**: "no other test touched" was
  false (the s215 suite gained the guard's 5 tests — additive, but undisclosed), and the classic
  systemsExplore corpus is 5 specs, not 6.

One gate catch during the chain: the build failed on a `require()`-style import in the new guard
test (the repo's own no-require lint) — and the file already imported `readFileSync` from
`node:fs` twelve lines up. Fixed, build 0.

## 3. Validation (one serialized chain)

typecheck 0 · vitest **321 files / 12,910 tests, 0 failures** (components 83/1,318 · lib
210/7,308 · app+server+world+math 24/162 · solos 3,992 + 2 — one earlier component-batch run
predates the final test-file state; the touched suites re-ran green after) · validate:content
1,840/1,840 · lint:pedagogy 1,711/1,711 · registrations clean · engine-registration 127/127 ·
content proof **814/814** · hash proof 1,701/1,701 · build 0 · Playwright 115/115 (Trap-D
protocol) · fresh-extraction reprove at seal.

## 4. Metrics

| metric | value |
|---|---|
| Lessons gaining their first causal interaction | **1** (`tse-04-01`) |
| The reversal arc | complete as a PAIR: no-flip control + flip experiment |
| Escalations off Fable | **0** (no Opus/Sonnet; credits reserved) |
| Agents spent | 1 (the independent QA — non-negotiable) |
| Author-caught-by-own-gates incidents | 3 (transcription; inclusion pin; require lint) |
| Refusals | five-gate discipline held; no new candidates mined |
| Content changes | 1 file, 1 inserted step |

## 5. Open queue for the next reset (priority order)

1. distributionCompareLab misconception-contrast model (10 lessons, highest-reuse quality lift).
2. Build-a-set numberLineRay steps for ee-05-02 "Graphing Inequalities" (the engine's gallery
   mode; needs no new fields — but apply the five gates fresh, the lesson has a numberLinePlace
   step whose NOVELTY overlap must be adjudicated honestly).
3. algebraTiles area authoring for a second distribute lesson (a(x+b) shape only; factor mode
   still display-only pending a non-trivial start design).
4. The seven deferred engine gaps.
5. describeState coverage 84/127 — the panel's state line is null for a third of engines;
   worth one batched pass someday, not urgent.
