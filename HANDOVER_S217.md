# HANDOVER → Session 217

Read `SESSION216_EXECUTION_REPORT.md`, `SESSION216_FABLE_QA.md`,
`SESSION216_CONTENT_CHANGE_LEDGER.md`. Rubric: `docs/CAPABILITY_AXES.md`.

## 0. State

`numberLineRay` has its first authored user: `tse-04-02/i1b` (the sign-flip lesson), Fable-QA
ACCEPT at 9.3 with mathematics 10/10. `requireSolvedForm` (additive) makes solve/reduce tasks
gradable on that engine. The S215 twelve-engine err-debt is **dissolved** (3 regex false
positives, 8 legitimate via the live-cue mechanism the contract now checks, 1 row corrected) and
the debt list is empty with both guard directions live. Registry: covariationScrubber err 1
(A→B), numberLineRay mobile 3 (Σ20, A). Content baseline SESSION210_LESSON_HASHES.json, proof
**813/813**. Gates at seal: 321 files / 12,896 vitest · engine-registration 127/127 · Playwright
115/115 · build 0. Four lessons now carry program-authored causal interactions.

## 1. Restart priorities (in order)

1. **`tse-04-01` contrast step** — QA-nominated: `3x > 12 → x > 4` where the ray does NOT move,
   isolating the negative factor as the cause of reversal. Same packet shape as i1b; expect the
   begins-solved guard to require `requireSolvedForm` here too (coefficient 3 at start). Five
   gates first; the pair (tse-04-01 no-flip, tse-04-02 flip) is the complete predict→contrast arc.
2. **Reachability guard** (QA-flagged hazard): nothing checks coefficient 1 is REACHABLE from a
   spec's offered transforms — an author could ship an unsolvable `requireSolvedForm` item. Close
   it in `widgetIntegrityErrors` (walk the transform-generated coefficient group, or bound-check
   the multiplicative closure) before authoring the second such item.
3. **Pin-scope decision** (O2's seal disclosure): the "classic byte-identical" pins verify
   testids and control counts, NOT the SVG accessible name — the S215 label change altered
   classic specs' names and no pin caught it. Either widen the pins to include accessible names
   or rename the guarantee in the tests' own comments. Small, but it is a check whose scope is
   narrower than its claim — the exact S215 lesson.
4. **distributionCompareLab misconception model** — highest-reuse (10 lessons) engine whose err
   mechanism is ghost-only; a genuine wrong-state contrast would be the next quality lift.
5. **The seven deferred engine gaps** when quota allows (nested rules `dr-04-02`, u-substitution
   `in-05-02`, error propagation `dc-03-02`, growth race `dc-04-02`, movable interval, odometer,
   quotient mode).

## 2. What S216 adds to the pattern book

- **A refusal can be the most valuable deliverable in the session.** The implementor's packet
  refusal (begins-solved + grades-correct-untouched, with an alternatives table showing every
  variant fails worse) is what produced `requireSolvedForm` — and the step that then shipped at
  9.3. Packets must keep inviting refusal; planners must keep accepting it.
- **Matching counts from two instruments are not confirmation if the instruments share an
  assumption.** The "twelve" was agreed by two independent counters — both blind the same way
  (single-segment testid regex). Now recorded in the contract's own comment. When two checks
  agree, ask what they share before believing them.
- **A rubric with two earning mechanisms needs a contract that checks both.** The err contract
  checked ghosts only; eight engines earned their score the other way. Contracts must be derived
  from the rubric's full text, not its most greppable clause.
- Fable-only adjudication is cheap and decisive: twelve debts resolved for a handful of greps,
  two registry changes, zero implementor tokens.

## 3. Traps (A–H stand; nothing new)

A: Playwright reuses 3100. B: two solo vitest files. C: foreground batched vitest. D: servers
resurrect — fuser + Ready/EADDRINUSE check. E: mtime churn lies — hash against the seal. F: don't
pin true-at-the-time facts as invariants. G: assert on scripted replacements. H: widget-union
additions break four exhaustive switches by design.

## 4. Tripwires

`mmipTypes.ts` FROZEN. Content proof expects **813**. Engine registration 127. Debt list EMPTY —
additions require adjudication, retirements must be deliberate (both directions fail by name).
`GHOST_RE` is multi-segment (`[a-z][a-z-]*-ghost`); the err contract accepts ghost OR
MULTI_CONTROL membership (imported, not copied). `requireSolvedForm`: optional, no default;
begins-solved guard compares set AND form; target must be solved-form. numberLineRay transforms:
keep coefficient ladders reversible (exact-inverse pairs) until the reachability guard exists.
`console.error` trap opt-in, empty allowlist. Rating changes: rubric-cited, non-author
adjudication only.

## 5. Verification chain

Unchanged (Trap-D fuser protocol). Expect vitest ~12.9k, proof **813/813**, hash 1,701/1,701,
engine registration 127/127.
