# HANDOVER → Session 213

Read `SESSION212_EXECUTION_REPORT.md`, `SESSION212_CONTENT_CHANGE_LEDGER.md`, "# S212 REVIEW"
in `SESSION208_INTEGRATION_REVIEW.md`, and `docs/RSG_DECISIONS.md` (now the append-only decision
log).

## 0. State

MMIP v1: four engines on assembled seams; all nine motion semantics have shipping producers;
one shared morph/history implementation with a checked two-path invariant. algebraTiles is a
multiplication workspace (area mode: model+grader+widget proven, zero authored users).
systemsExplore is a breakable systems lab (editable lines, degenerate verdicts, envelope
persistence, edited-lines grading — zero authored users; validation REQUIRES
degenerateSystemFeedback before any spec can opt in). Content baseline
SESSION210_LESSON_HASHES.json (no change since S211's one variant key). Gates at S212 seal:
311 files / 12,542 vitest · Playwright 115/115 · hash 1,701/1,701 · proof 809/809 · build 0.

## 1. Restart priorities

1. **First authored content for the two proven capabilities** (content session, freeze
   protocol): an `area`-mode distribute step (reads naturally after tse-01-01 per O1) and a
   breakable-systems step (author degenerateSystemFeedback — validation enforces it). Batch
   with any further DEMAND-gated rich-mix passes (vectors/similarity clusters per S209).
2. **Hoist `accept()` into mmipTypes.ts** beside rejectTransaction (algebraTiles and
   solveBalance carry the same line; a third engine will copy it).
3. **Hunt the swallowed React `NaN for y1/y2` warning** (pre-existing, green-suite-swallowed);
   consider a vitest setup that fails on unexpected console.error.
4. Deferred: compile-time useKeyedMorphHistory/useExternalMorphHistory split; geometry studio
   flagships (next P1 family by mandate order); eight engine gaps; world parity (P3).

## 2. Traps (stable across four sessions — short form)

A: Playwright reuses 3100 prod server. B: variants.test.ts + content.widgets.audit.test.ts
solo. C: foreground directory-batched vitest, dot reporter, never backgrounded. D: servers
resurrect — `fuser 3100/tcp` to find/kill; require Ready + zero EADDRINUSE in the new server's
own log. E: mtime churn lies — hash against the sealed tarball. Co-work: single-writer locks,
sequential widgets.tsx windows, one serialized gate chain, adversarial review before every seal
(S212 catches: a shippable borrowed-feedback state → made unreachable by validation; an
unpinned learner-audible behavior change → pinned deliberately).

## 3. Tripwires

mmipTypes.ts FROZEN (five seals). Hash target SESSION210 manifest; AUTHORIZED map path-keyed,
cumulative. widgetMorph history: keyed/external latch throws on mixing; coalesce-on-empty
throws. Persisted-value tripwires: leCanonicalFor, SlopeTriangleW, algebraTiles {x,c,mat}
duality, systemsExplore lines envelope (single writer systemsPairPersist, only-on-difference).
systemsExploreEditErrors: editable ⇒ degenerateSystemFeedback required — do not relax.
keyboardParityCheck: native disabled only. lookupOrThrow for learner-visible table lookups.
Rating lifts: S205M rubric only. describeState narration: standing product decision.
algebraTiles has NO reset control — a sentinel test guards the control set; adding one obliges
the widget-audible reset pin (see widgets.mmip.o1.s212 notes).

## 4. Verification chain

Unchanged (HANDOVER_S211 §4 + Trap D fuser protocol). Expect vitest ~12.5k, proof 809/809,
hash 1,701/1,701 vs SESSION210 manifest.
