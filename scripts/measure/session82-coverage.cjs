const fs=require('fs'),path=require('path'),assert=require('assert'),ts=require('/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript/lib/typescript.js');
require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS,esModuleInterop:true}}).outputText,f);
const V=require('../../src/lib/variants.ts');const rows=[];let overallTotal=0,overallServed=0,g6Total=0,g6Served=0;
for(const e of fs.readdirSync('content/courses',{withFileTypes:true}).filter(e=>e.isDirectory())){const dir=path.join('content/courses',e.name),metaPath=path.join(dir,'course.json'),lessons=path.join(dir,'lessons');if(!fs.existsSync(metaPath)||!fs.existsSync(lessons))continue;const meta=JSON.parse(fs.readFileSync(metaPath,'utf8'));let total=0,served=0;for(const f of fs.readdirSync(lessons).filter(f=>f.endsWith('.json'))){const d=JSON.parse(fs.readFileSync(path.join(lessons,f),'utf8'));for(const s of d.steps||[])if(['check','challenge'].includes(s.kind)){total++;if(V.variantForStep(s,`coverage:${e.name}:${f}:${s.id}`,'core'))served++;}}rows.push({id:e.name,title:meta.title,grade:meta.gradeLevel,total,served,gaps:total-served});overallTotal+=total;overallServed+=served;if(meta.gradeLevel===6){g6Total+=total;g6Served+=served;}}
assert.deepStrictEqual({total:overallTotal,served:overallServed},{total:4471,served:2054});assert.deepStrictEqual({total:g6Total,served:g6Served},{total:303,served:244});const target=rows.find(r=>r.id==='ratios-rates');assert.deepStrictEqual({total:target.total,served:target.served,gaps:target.gaps},{total:61,served:61,gaps:0});const g6=rows.filter(r=>r.grade===6).sort((a,b)=>a.gaps-b.gaps||a.id.localeCompare(b.id));console.log(JSON.stringify({overall:{total:overallTotal,served:overallServed,gaps:overallTotal-overallServed,percent:+(100*overallServed/overallTotal).toFixed(2)},grade6:{total:g6Total,served:g6Served,gaps:g6Total-g6Served,percent:+(100*g6Served/g6Total).toFixed(2)},grade6Courses:g6,status:'PASS'},null,2));
