const fs=require('fs'),assert=require('assert'),ts=require('/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript/lib/typescript.js');
require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS,esModuleInterop:true}}).outputText,f);
const V=require('../../src/lib/variants.ts');
function loadEvaluate(){const src=fs.readFileSync('src/lib/evaluate.ts','utf8');const js=ts.transpileModule(src,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS,esModuleInterop:true}}).outputText;const mod={exports:{}};const custom=(id)=>{if(id==='./schema')return {columnCalcTruth(){throw new Error('unused')},mixedRegroupTruth(){throw new Error('unused')}};if(id==='./mathUtils')return require('../../src/lib/mathUtils.ts');return require(id)};new Function('module','exports','require','__filename','__dirname',js)(mod,mod.exports,custom,'src/lib/evaluate.ts','src/lib');return mod.exports.evaluate;}
const evaluate=loadEvaluate();
const forms=[
['unit-frac-divide','sameDenomCount'],['unit-frac-divide','divisionMagnitude'],['unit-frac-divide','properPieces'],['unit-frac-divide','divideFractionWhole'],['unit-frac-divide','divideFractionNumerator'],['unit-frac-divide','flipDivisor'],['unit-frac-divide','mixedDivide'],['unit-frac-divide','mixedScoops'],['long-div-2digit','invalidRemainder'],['decimal-align-addsub','threeAdd'],['lcm-pair','gcfPair'],['lcm-pair','relativelyPrime'],['lcm-pair','gcfContext'],['lcm-pair','gcfThree'],['distributive','factorMissing'],['distributive','factorError'],['distributive','factorGcf'],['distributive','factorEvaluate'],['negative-intro','compareNegatives'],['negative-intro','colderNegatives'],['negative-intro','compareMixedSign'],['negative-intro','orderSignedFour'],['negative-intro','absoluteNumeric'],['negative-intro','absoluteEqual'],['negative-intro','absoluteDifference'],['negative-intro','fartherAbsolute'],['negative-intro','debtAbsolute'],['negative-intro','coldestSigned'],['negative-intro','deeperSigned'],['negative-intro','compareNegativeFractions'],['negative-intro','compareNegativeMixed'],['negative-intro','compareFractionDecimal'],['negative-intro','orderRationalsFive'],['coordinate-plot','signedQuadrant'],['coordinate-plot','axisLocation'],['coordinate-plot','reflectXAxis'],
];
let builds=0,assertions=0;
for(const [gen,form] of forms)for(const band of ['support','core','stretch'])for(let i=0;i<170;i++){const v=V.variantForGenForm(gen,form,`session81-eval:${gen}:${form}:${band}:${i}`,band),w=v.widget;builds++;
 if(w.type==='numeric'){let r=evaluate(w,w.answer);assert(r.correct);assertions++;for(const e of w.commonErrors){r=evaluate(w,e.value);assert(!r.correct);assert.equal(r.feedback,e.feedback);assertions+=2;}r=evaluate(w,w.answer+0.314159);assert(!r.correct);assertions++;}
 else if(w.type==='mcq'){for(const o of w.options){const r=evaluate(w,o.id);assert.equal(r.correct,o.correct);assert.equal(r.feedback,o.feedback);assertions+=2;}}
 else if(w.type==='rationalCompare'){for(const rel of ['lt','eq','gt']){const r=evaluate(w,rel);assert.equal(r.correct,rel===w.answer);if(rel!==w.answer)assert.equal(r.feedback,{lt:w.ltFeedback,eq:w.eqFeedback,gt:w.gtFeedback}[rel]);assertions+=2;}}
 else if(w.type==='dragOrder'){let r=evaluate(w,w.correctOrder);assert(r.correct);assertions++;const wrong=[...w.correctOrder].reverse();r=evaluate(w,wrong);assert(!r.correct);assertions++;}
 else if(w.type==='absValueLine'){let r=evaluate(w,w.answerId);assert(r.correct);assertions++;for(const item of w.items)if(item.id!==w.answerId){r=evaluate(w,item.id);assert(!r.correct);assert.equal(r.feedback,item.feedback);assertions+=2;}if(w.answerId!=='equal'&&w.equalLabel){r=evaluate(w,'equal');assert(!r.correct);assert.equal(r.feedback,w.equalFeedback);assertions+=2;}}
 else throw new Error(`unexpected ${w.type}`);
}
console.log(JSON.stringify({forms:forms.length,builds,assertions,status:'PASS'}));
