import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../..");const dir=path.join(root,"content","courses","measurement-data","lessons");const check=process.argv.includes("--check");
const repairs=[
["CHOICE-0167","md-01-01","ch1",["2:20 — Dee swapped the hands (and read the 2-mark as ten minutes)"],["2:20 — the hour and minute hands were swapped.","4:10 — the hands are in their correct places.","2:04 — each clock number is one minute.","4:02 — the short hand sets the minutes."]],
["CHOICE-0168","md-01-01","k3",["Halfway between the 7 and the 8","Exactly on the 7","On the 6","Exactly on the 8"],["Halfway between the 7 and 8","Exactly on the 7 mark","Exactly on the 6 mark","Exactly on the 8 mark"]],
["CHOICE-0169","md-01-02","k3",["Yes — quarter TO means 15 minutes before 3:00, which is 2:45"],["Yes — quarter to 3 is 15 minutes before 3:00.","No — quarter to 3 is 15 minutes after 3:00.","No — quarter to 3 is 45 minutes before 3:00.","No — a quarter-hour has 25 minutes before 3:00."]],
["CHOICE-0170","md-01-03","k3",["Hop to 7:00 (that's 25 minutes), then 10 more — total 35"],["Hop 25 minutes to 7:00, then add 10 more minutes.","Subtract 10 from 35 because the clock passes 7:00.","Count every minute because clocks cannot cross an hour.","Treat 7:10 as 70 minutes, then subtract 35."]],
["CHOICE-0171","md-03-01","k3",["So a few symbols can stand for a big count — the graph stays small"],["A key lets a few symbols show a large count clearly.","A key makes every graph harder to read, even with large counts.","A key stops pictures from showing even one vote clearly.","A key is only decoration, not part of reading a graph."]],
["CHOICE-0172","md-03-02","k1",["Tuesday and Wednesday tie for the most"],["Tuesday and Wednesday tie for the most books read.","Thursday alone had the most books read.","Monday had more books than Tuesday.","Every day had the same number of books."]],
["CHOICE-0173","md-03-02","k3",["So big amounts fit — a bar of 80 needs only 8 grid steps, not 80"],["A scale of 10s fits large amounts into fewer grid steps.","A scale of 10s makes all graph readers confused.","A scale of 10s cannot show smaller amounts accurately.","A scale of 10s changes what every bar represents."]],
["CHOICE-0174","md-05-03","k2",["The 2-by-8 — it needs 20 of fence vs the square's 16"],["The 2-by-8 garden needs 20 units of fence.","The 4-by-4 garden needs more than 20 units.","Both gardens need 16 units of fence around their edges.","Neither garden needs fencing around its edges."]]
];
let changed=0;for(const [id,lessonId,stepId,before,after] of repairs){const file=path.join(dir,`${lessonId}.json`),lesson=JSON.parse(await readFile(file,"utf8")),step=lesson.steps.find(s=>s.id===stepId),options=step?.widget?.options;if(!step||step.widget?.type!=="mcq"||JSON.stringify(options.map(o=>o.id))!==JSON.stringify(["a","b","c","d"])||JSON.stringify(options.filter(o=>o.correct).map(o=>o.id))!==JSON.stringify(["a"]))throw new Error(`${id}: evaluator drift`);const labels=options.map(o=>o.label);if(JSON.stringify(labels)===JSON.stringify(after))continue;if(!before.includes(labels[0]))throw new Error(`${id}: source drift`);options.forEach((o,i)=>o.label=after[i]);changed++;if(!check)await writeFile(file,`${JSON.stringify(lesson,null,2)}\n`);}
if(check&&changed)throw new Error(`S292 needs ${changed} repairs`);console.log(JSON.stringify({course:"measurement-data",signedRootCauseClosures:repairs.length,changed,current:changed===0}));
