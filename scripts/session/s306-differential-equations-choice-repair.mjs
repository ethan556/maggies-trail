/** S306 — source-local Differential Equations MCQ choice-parity repair. */
import { createHash } from "node:crypto";
import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");
const directory = path.join(root, "content", "courses", "differential-equations", "lessons");
const check = process.argv.includes("--check");
const hash = (value) => createHash("sha256").update(JSON.stringify(value)).digest("hex");
const sourceJson = (value) => JSON.stringify(value).replace(/[^\x20-\x7e]/g, (character) => `\\u${character.codePointAt(0).toString(16).padStart(4, "0")}`);
const escape = (value) => value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");

const choices = Object.freeze([
  { workId: "CHOICE-0034", lessonId: "de-01-01", stepId: "k2", kind: "check", evaluatorHash: "f1630dce1bcd7460bde3c6f3adc8d96d1cefc371a2f06ad251689b4104d3de8c", feedbackHash: "9b1b019e44dba35475902b64375898756de3f5e1a4facde395a6bfdb4eb7cd0d", labels: [["o1", "Infinitely many — one through every starting point. The initial condition picks one.", "Infinitely many: each starting point gives a different solution curve."], ["o2", "Exactly one.", "Exactly one: a slope field determines a single global curve."], ["o3", "None, until you solve it algebraically.", "None: algebraic solving must happen before curves can appear."], ["o4", "Two — one going each way.", "Two: one solution follows each direction through every point."]] },
  { workId: "CHOICE-0035", lessonId: "de-01-01", stepId: "k3", kind: "check", evaluatorHash: "aacdf3d11e5de535c58b34548f9890de0632afcc413cb8c94fa2cc202bcf4d5b", feedbackHash: "577aa61a01d7983010eb86eb381db3106fb5771064a3208f00dd24e327a97df8", labels: [["o1", "The segments get STEEPER — the bigger y is, the faster it grows.", "Segments get steeper because slope 0.5y increases with y."], ["o2", "They get flatter.", "Segments flatten because larger y makes the slope smaller."], ["o3", "Nothing changes.", "Segments stay unchanged because slope ignores the value of y."], ["o4", "They turn downward.", "Segments turn downward because positive y reverses the slope."]] },
  { workId: "CHOICE-0036", lessonId: "de-01-02", stepId: "k2", kind: "check", evaluatorHash: "706ebbe5e742b1a22bd63df156810a9a60da330ee3609ef80a729c82f9e2544e", feedbackHash: "0e4a28dead9f7c456f40065d47afea28a7b4a2bdbbc31a92a4ae78bfdce3ba5c", labels: [["o1", "y = 4 is stable (it attracts); y = 0 is unstable (it repels).", "y = 4 is stable and y = 0 is unstable, based on their nearby motion."], ["o2", "Both are stable.", "Both are stable because nearby solutions move toward both levels."], ["o3", "Both are unstable.", "Both are unstable because nearby solutions move away from both levels."], ["o4", "y = 0 is stable; y = 4 is unstable.", "y = 0 is stable and y = 4 is unstable, based on their nearby motion."]] },
  { workId: "CHOICE-0037", lessonId: "de-02-01", stepId: "ch1", kind: "challenge", evaluatorHash: "4346ceda9b0012bb72569895fce477e5cc43d1b91e583078b86c3ff71603b9e7", feedbackHash: "1b0eda4309ece33a9956396b8eada6a3f5b1c05f63ad43a7f9c36855ac247fe4", labels: [["o1", "y = 0 — the equilibrium. Both sides are zero there, so it IS a solution, but dividing by y excluded it.", "y = 0; it solves the equation but dividing by y removes it."], ["o2", "None — the family covers everything.", "No solution is lost because the separated family includes all cases."], ["o3", "y = 1", "y = 1 is lost because it makes both sides of the equation zero."], ["o4", "The negative solutions.", "All negative solutions are lost when dividing by a positive y."]] },
  { workId: "CHOICE-0038", lessonId: "de-02-01", stepId: "k2", kind: "check", evaluatorHash: "4d1c29f0c7bfbe878fb126d8e40edd2814626d4fb6e6fdab31ace4664d319030", feedbackHash: "eb1daaf35f02df66a9a1672c0d095d5ddeaaf255edcb0e2991c4281279805743", labels: [["o1", "y = Ae^(x²), where A = e^C", "y = Ae^(x²), with A = e^C absorbing the constant."], ["o2", "y = x² + C", "y = x² + C, keeping the logarithm as a linear expression."], ["o3", "y = e^(x²) + C", "y = e^(x²) + C, adding the constant after exponentiating."], ["o4", "y = ln(x² + C)", "y = ln(x² + C), moving the logarithm to the other side."]] },
  { workId: "CHOICE-0039", lessonId: "de-03-01", stepId: "ch1", kind: "challenge", evaluatorHash: "f7d37dc06a898ce1beee314ae8788441c2072b2faade6578aebc50fedaa2d9d7", feedbackHash: "3b976f8863abd0df3a02759b90a76b8c6749ef350bec3bbac7ee93c8fb4d187d", labels: [["o1", "Neither — the half-life is the same. The y₀ cancels: T = ln2/k.", "Neither; the same k gives both samples the same half-life."], ["o2", "The kilogram — it decays faster.", "Kilogram sample; more mass makes its half-life shorter."], ["o3", "The microgram — less to get through.", "Microgram sample; less mass makes its half-life shorter."], ["o4", "It depends on k.", "Both depend on k, so their half-lives cannot be compared."]] },
  { workId: "CHOICE-0040", lessonId: "de-03-02", stepId: "k1", kind: "check", evaluatorHash: "58bec45714dc765b9d26fd69d26ff2f85c301ceee26df7cdd2ec091f29743ed1", feedbackHash: "4cd942c9fcf3260571083ab7203b6687e055f834ea3107224b54b77f855f2acb", labels: [["o1", "The bracket is nearly 1, so dP/dt ≈ kP — it grows exponentially.", "Growth is nearly exponential because 1 − P/K is close to 1."], ["o2", "Growth stops.", "Growth stops because a very small P makes the rate zero."], ["o3", "The population falls.", "Population falls because a small P makes the bracket negative."], ["o4", "It grows linearly.", "Growth is linear because kP has no exponential behavior."]] },
  { workId: "CHOICE-0041", lessonId: "de-03-02", stepId: "k3", kind: "check", evaluatorHash: "48ba349b781e1a91cdd0a2affaa95c855d6a244f07246f9d9124d17132a49a86", feedbackHash: "d2317c930f2f59b9199d7bf2c5f59a1bea3e7cfcef78e1b2fffbf72291b2ed14", labels: [["o1", "Still growing, but ever more slowly — easing toward the ceiling.", "Growing more slowly because it is approaching K from below."], ["o2", "Shrinking.", "Shrinking because values above K/2 always decrease to zero."], ["o3", "Growing faster and faster.", "Growing faster because the rate rises as P nears K."], ["o4", "Holding steady.", "Holding steady because every value below K is equilibrium."]] },
  { workId: "CHOICE-0042", lessonId: "de-04-01", stepId: "k3", kind: "check", evaluatorHash: "ab10830e86a5eed1ead75173b24087d45b2fcf6c7a840c98057c8952112c0b2f", feedbackHash: "da597bb69d798b1a836ab9fd11ed38db3579e5e7edd9c27337587d8eeab81adb", labels: [["o1", "y = eˣ is concave UP, so every tangent step lies below the curve.", "eˣ is concave up, so Euler’s tangent steps lie below the curve."], ["o2", "Because h is too big.", "Step size h is too big, so every Euler estimate must undershoot."], ["o3", "Because e is irrational.", "e is irrational, so Euler’s decimal estimate must be too small."], ["o4", "An arithmetic slip.", "An arithmetic slip caused the exact answer e to be larger."]] },
]);

const files = new Map();
async function source(lessonId) { const file = path.join(directory, `${lessonId}.json`); if (!files.has(file)) files.set(file, await readFile(file, "utf8")); return [file, files.get(file)]; }
function lesson(text) { return JSON.parse(text); }
function findStep(raw, stepId) { const found = raw.steps.find((entry) => entry.id === stepId); if (!found) throw new Error(`missing ${raw.id}/${stepId}`); return found; }
function evaluator(widget) { const { prompt: _prompt, options, ...rest } = widget; return { ...rest, options: options.map(({ label: _label, ...option }) => option) }; }
function replaceOnce(text, pattern, replacement, label) { let count = 0; const updated = text.replace(pattern, (...matches) => { count += 1; return replacement.replace(/\$(\d+)/g, (_, index) => String(matches[Number(index)] ?? "")); }); if (count !== 1) throw new Error(`${label}: expected one raw source match, found ${count}`); return updated; }
for (const target of choices) { const [, text] = await source(target.lessonId); const step = findStep(lesson(text), target.stepId); const widget = step.widget; if (step.kind !== target.kind || !widget || widget.type !== "mcq" || !Array.isArray(widget.options)) throw new Error(`${target.workId}: expected ${target.kind} MCQ`); if (hash(evaluator(widget)) !== target.evaluatorHash || hash(widget.options.map((option) => [option.id, option.feedback ?? null])) !== target.feedbackHash) throw new Error(`${target.workId}: evaluator or feedback drift`); if (widget.options.filter((option) => option.correct).map((option) => option.id).join(",") !== "o1") throw new Error(`${target.workId}: correct-answer drift`); for (const [optionId, before, after] of target.labels) { const option = widget.options.find((candidate) => candidate.id === optionId); if (!option || (option.label !== before && option.label !== after)) throw new Error(`${target.workId}/${optionId}: label drift`); } }
let changed = 0;
for (const target of choices) { const [file] = await source(target.lessonId); let text = files.get(file); for (const [optionId, before, after] of target.labels) { if (findStep(lesson(text), target.stepId).widget.options.find((option) => option.id === optionId).label === after) continue; const unicodeLiteral = JSON.stringify(before); const beforeLiteral = text.includes(unicodeLiteral) ? unicodeLiteral : sourceJson(before); const afterLiteral = beforeLiteral === unicodeLiteral ? JSON.stringify(after) : sourceJson(after); const pattern = new RegExp(`(\\{\\s*"id"\\s*:\\s*"${escape(target.stepId)}"[\\s\\S]*?"id"\\s*:\\s*"${escape(optionId)}"\\s*,\\s*"label"\\s*:\\s*)${escape(beforeLiteral)}`); text = replaceOnce(text, pattern, `$1${afterLiteral}`, `${target.workId}/${optionId}`); changed += 1; } files.set(file, text); }
if (check && changed) throw new Error(`S306 needs ${changed} source repair(s)`);
if (!check && changed) for (const [file, text] of files) await writeFile(file, text);
const packetSeal = createHash("sha256").update([...files].sort(([a], [b]) => a.localeCompare(b)).map(([file, text]) => `${path.basename(file)}\0${text}`).join("\n")).digest("hex");
console.log(JSON.stringify({ course: "differential-equations", choiceRows: choices.length, labelEdits: changed, current: changed === 0, packetSeal }, null, 2));
