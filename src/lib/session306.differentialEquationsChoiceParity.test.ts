import { createHash } from "node:crypto";
import { readFile, readdir } from "node:fs/promises";
import path from "node:path";
import { describe, expect, it } from "vitest";
import { evaluate } from "./evaluate";
import { Lesson, WidgetSpec } from "./schema";
type RawStep = { id: string; kind: string; widget?: unknown }; type RawLesson = { id: string; courseId: string; steps: RawStep[] };
const course = path.join(process.cwd(), "content", "courses", "differential-equations"); const directory = path.join(course, "lessons"); const hash = (value: unknown) => createHash("sha256").update(JSON.stringify(value)).digest("hex");
const choices = [
  ["de-01-01","k2","check","f1630dce1bcd7460bde3c6f3adc8d96d1cefc371a2f06ad251689b4104d3de8c","9b1b019e44dba35475902b64375898756de3f5e1a4facde395a6bfdb4eb7cd0d",["Infinitely many: each starting point gives a different solution curve.","Exactly one: a slope field determines a single global curve.","None: algebraic solving must happen before curves can appear.","Two: one solution follows each direction through every point."]],
  ["de-01-01","k3","check","aacdf3d11e5de535c58b34548f9890de0632afcc413cb8c94fa2cc202bcf4d5b","577aa61a01d7983010eb86eb381db3106fb5771064a3208f00dd24e327a97df8",["Segments get steeper because slope 0.5y increases with y.","Segments flatten because larger y makes the slope smaller.","Segments stay unchanged because slope ignores the value of y.","Segments turn downward because positive y reverses the slope."]],
  ["de-01-02","k2","check","706ebbe5e742b1a22bd63df156810a9a60da330ee3609ef80a729c82f9e2544e","0e4a28dead9f7c456f40065d47afea28a7b4a2bdbbc31a92a4ae78bfdce3ba5c",["y = 4 is stable and y = 0 is unstable, based on their nearby motion.","Both are stable because nearby solutions move toward both levels.","Both are unstable because nearby solutions move away from both levels.","y = 0 is stable and y = 4 is unstable, based on their nearby motion."]],
  ["de-02-01","ch1","challenge","4346ceda9b0012bb72569895fce477e5cc43d1b91e583078b86c3ff71603b9e7","1b0eda4309ece33a9956396b8eada6a3f5b1c05f63ad43a7f9c36855ac247fe4",["y = 0; it solves the equation but dividing by y removes it.","No solution is lost because the separated family includes all cases.","y = 1 is lost because it makes both sides of the equation zero.","All negative solutions are lost when dividing by a positive y."]],
  ["de-02-01","k2","check","4d1c29f0c7bfbe878fb126d8e40edd2814626d4fb6e6fdab31ace4664d319030","eb1daaf35f02df66a9a1672c0d095d5ddeaaf255edcb0e2991c4281279805743",["y = Ae^(x²), with A = e^C absorbing the constant.","y = x² + C, keeping the logarithm as a linear expression.","y = e^(x²) + C, adding the constant after exponentiating.","y = ln(x² + C), moving the logarithm to the other side."]],
  ["de-03-01","ch1","challenge","f7d37dc06a898ce1beee314ae8788441c2072b2faade6578aebc50fedaa2d9d7","3b976f8863abd0df3a02759b90a76b8c6749ef350bec3bbac7ee93c8fb4d187d",["Neither; the same k gives both samples the same half-life.","Kilogram sample; more mass makes its half-life shorter.","Microgram sample; less mass makes its half-life shorter.","Both depend on k, so their half-lives cannot be compared."]],
  ["de-03-02","k1","check","58bec45714dc765b9d26fd69d26ff2f85c301ceee26df7cdd2ec091f29743ed1","4cd942c9fcf3260571083ab7203b6687e055f834ea3107224b54b77f855f2acb",["Growth is nearly exponential because 1 − P/K is close to 1.","Growth stops because a very small P makes the rate zero.","Population falls because a small P makes the bracket negative.","Growth is linear because kP has no exponential behavior."]],
  ["de-03-02","k3","check","48ba349b781e1a91cdd0a2affaa95c855d6a244f07246f9d9124d17132a49a86","d2317c930f2f59b9199d7bf2c5f59a1bea3e7cfcef78e1b2fffbf72291b2ed14",["Growing more slowly because it is approaching K from below.","Shrinking because values above K/2 always decrease to zero.","Growing faster because the rate rises as P nears K.","Holding steady because every value below K is equilibrium."]],
  ["de-04-01","k3","check","ab10830e86a5eed1ead75173b24087d45b2fcf6c7a840c98057c8952112c0b2f","da597bb69d798b1a836ab9fd11ed38db3579e5e7edd9c27337587d8eeab81adb",["eˣ is concave up, so Euler’s tangent steps lie below the curve.","Step size h is too big, so every Euler estimate must undershoot.","e is irrational, so Euler’s decimal estimate must be too small.","An arithmetic slip caused the exact answer e to be larger."]],
] as const;
async function load(id: string) { return JSON.parse(await readFile(path.join(directory, `${id}.json`), "utf8")) as RawLesson; } function step(current: RawLesson, id: string) { const found = current.steps.find((entry) => entry.id === id); if (!found) throw new Error(`${current.id}/${id} missing`); return found; } function evaluator(widget: { prompt?: string; options: Array<{ label: string; [key: string]: unknown }>; [key: string]: unknown }) { const { prompt: _prompt, options, ...rest } = widget; return { ...rest, options: options.map(({ label: _label, ...option }) => option) }; }
describe("S306 Differential Equations choice parity", () => {
  it("preserves all nine evaluator, feedback, identity, and answer contracts", async () => { for (const [lessonId, stepId, kind, evaluatorHash, feedbackHash, labels] of choices) { const current = step(await load(lessonId), stepId); expect(current.kind).toBe(kind); const rawWidget = current.widget as { type?: string; prompt?: string; options?: Array<{ id: string; label: string; correct: boolean; feedback?: string; [key: string]: unknown }>; [key: string]: unknown }; expect(rawWidget.type).toBe("mcq"); if (!rawWidget.options) throw new Error(`${lessonId}/${stepId}: missing options`); expect(hash(evaluator(rawWidget as Required<typeof rawWidget>))).toBe(evaluatorHash); expect(hash(rawWidget.options.map((option) => [option.id, option.feedback ?? null]))).toBe(feedbackHash); const widget = WidgetSpec.parse(current.widget); expect(widget.type).toBe("mcq"); if (widget.type !== "mcq") throw new Error(`${lessonId}/${stepId}: expected MCQ`); expect(widget.options.map((option) => option.id)).toEqual(["o1","o2","o3","o4"]); expect(widget.options.map((option) => option.label)).toEqual(labels); expect(widget.options.filter((option) => option.correct).map((option) => option.id)).toEqual(["o1"]); for (const option of widget.options) expect(evaluate(widget, option.id).correct, `${lessonId}/${stepId}/${option.id}`).toBe(option.correct); const lengths = widget.options.map((option) => option.label.length); expect(Math.max(...lengths)-Math.min(...lengths),`${lessonId}/${stepId}`).toBeLessThanOrEqual(12); } });
  it("keeps the complete course manifest and valid lesson schemas", async () => { const manifest = JSON.parse(await readFile(path.join(course,"course.json"),"utf8")) as { chapters: Array<{ lessonIds: string[] }> }; const declared = manifest.chapters.flatMap((chapter) => chapter.lessonIds).sort(); const files = (await readdir(directory)).filter((file) => file.endsWith(".json")).sort(); expect(files.map((file) => file.replace(/\.json$/,""))).toEqual(declared); for (const file of files) { const raw=JSON.parse(await readFile(path.join(directory,file),"utf8")) as RawLesson; expect(file).toBe(`${raw.id}.json`); expect(raw.courseId).toBe("differential-equations"); Lesson.parse(raw); } });
});
