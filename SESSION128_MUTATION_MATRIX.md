# Session 128 adversarial mutation matrix

| mutation | detector | expected failure |
|---|---|---|
| Change a converted ruler's `objectEnd` without changing the frozen answer | `content-change-proof-s128.mjs`; `session128.reuse.test.ts` | after-image hash and independent answer mismatch |
| Delete one `commonPlacements` entry | content proof; reuse audit; targeted Vitest | preserved misconception missing |
| Make a common placement equal the correct count | `widgetIntegrityErrors`; targeted Vitest | success-state misconception rejected |
| Cap ruler placement at `requiredPlacements + 2` again | `reuse-wave-s128.mjs` static source contract | named end-label/sum errors no longer guaranteed reachable |
| Remove the `Remove unit` control | reuse audit; keyboard test | irreversible learner construction |
| Grade common placements with generic gap feedback | reuse audit; targeted evaluator test | misconception-specific feedback lost |
| Convert `mmt-02-01` to `estimateSlider` | targeted rejection test; disposition parity | discrete answer set widened to an interval |
| Convert `dop-01-02` to current `evalOrder` | `evalOrderReachable` targeted test | authored wrong values 14/24 unreachable |
| Convert `rr-03-03` to `covariationScrubber` | targeted rejection test | moving the already-given input solves the step |
| Change any non-target lesson step | content-change proof | non-target step hash mismatch |
| Change lesson prose, IDs, order, hints, explanations, concept tags, or prediction | content-change proof | frozen surface/top-level/order hash mismatch |
| Leave completed lessons in the reviewed C/D policy | excellence compiler live-policy parity | stale policy row failure |
| Hard-code the historical backlog length at 64 | excellence compiler | progressive queue cannot close; source audit regression |
