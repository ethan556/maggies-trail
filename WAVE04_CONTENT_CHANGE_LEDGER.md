# Wave 04 content change ledger — S224

| Lesson | Class | Before | After | Retrieval protected | Mathematical verification |
|---|---|---|---|---|---|
| `ep-02-01` Degree & Like Terms | A — concept acquisition | One MCQ identified a like pair | Learner sorts six signed terms into `x²`, `x`, and constant equivalence classes; diagnostic feedback distinguishes coefficient/sign from variable signature | Yes; degree, coefficient, constant, leading coefficient, challenge, and remedial checks are unchanged | Like terms share the complete variable/exponent signature; sign and coefficient do not change the family |
| `dr-03-02` The Quotient Rule | A — concept acquisition | Three passive panels stated where minus and square come from | Learner varies `u′` and `v′`; the model computes `u′v`, `uv′`, their ordered difference, `v²`, and the resulting rate | Yes; all four independent numeric/retrieval checks remain | For `u=6`, `v=4`, `u′=3`, `v′=1`: `(3·4−6·1)/4²=6/16=0.375` |

No curriculum mathematics was altered in `ep-01-02`; its authored caret shorthand is converted to TeX only at render time.

## S225 batch 2

| Lesson | Class | Before | After | Retrieval protected | Mathematical verification |
|---|---|---|---|---|---|
| `in-05-01` Undoing the Chain Rule | A - concept acquisition | Passive reveal stated the forward chain rule | Learner synchronizes `2x(x^2+1)^3 dx` with `u=x^2+1`, `du=2x dx`, and `u^3 du`; live KaTeX/MathML shows both worlds and enforces the no-stranded-`x` invariant | Yes; every independent integration check, challenge, and remedial step is unchanged | With coefficient 2 and power 3, `2x dx=du`, so the transformed integral is `integral u^3 du = (1/4)u^4+C` |
| `ca-03-01` Rolle's Theorem | A - concept acquisition | Passive theorem panels followed one numeric answer | Learner moves endpoint B on `f(x)=x^2-4x` from 1.5 to 4 and watches endpoint heights, secant slope, and the interior tangent synchronize | Yes; theorem hypotheses, root-count, cubic, and challenge/retrieval checks are unchanged | `f(0)=f(4)=0`; secant slope is 0; `f'(x)=2x-4`, so the guaranteed flat point is `c=2` |

No curriculum mathematics was changed. This batch changes representation, manipulation, and feedback only.
