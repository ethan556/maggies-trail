# HANDOVER — COWORK S243

**State:** implementation branch `codex/v4-s243-implementation` · **Base:**
`0cedbace4c29faff3e20e53e2e80670674f4bdd2` (`origin/main`) · **Deployment:** not promoted.

## Outcome

S243 implements bounded V4 canaries, not the full Maths Content Excellence Program V4. No authored
lesson JSON changed, no declaration was added, and no avatar was released. Focused tests, content
gates, typecheck, build, and local browser checks support the canaries, but all four mandatory shards
still contain failures, so this is not a green landing.

## Implemented canaries

1. Grade-K generators: positive joining/separating actions, named actors, age-appropriate complete
   stems, singular/plural agreement, parallel mathematical choices without filler, valid removal
   quantities, and matching independent parsing.
2. `distributionCompareLab` judge choices: deterministic seed shuffle with stable grading IDs and
   native keyboard order. Numeric measure choices retain semantic order.
3. Homepage: canonical catalogue totals render on the first server frame, with no zero flash.
4. Catalogue: accessible level filters, scoped search, live state, and placement route.
5. Basecamp: clear overview/start language and explicitly optional recommended prerequisites.
6. Achievements: typed owned icons replace emoji; earned/locked states are accessible.
7. Avatars: production-safety validators plus pre-canary measurement. Four candidates remain
   quarantined; the enabled allowlist is empty and `public/avatars/` holds only the placeholder.
8. CML: Windows-safe URL/path resolution and normalized report paths.
9. Repetition scoring: contrast/transfer require genuinely different representations. The resulting
   83-row K–8 queue is narrowly and evidenceably classified (72 multi-engine, 11 extend).

## Exact validation evidence

Green: integrated packet **84/84** after harness correction; later core packet **45/45** plus scorer
**10/10**; Grade-K language + full variants **4,001/4,001**; independent variants **7,126/7,126**;
excellence backlog **4/4**; typecheck **EXIT 0**; schema **1,840/1,840**; pedagogy
**1,711/1,711**; registration consistent; build **EXIT 0**; strict CML **0 errors / 200 warnings**
(161 prediction-not-causal, 39 flagship-response-heavy). Native validation found only generated
`node_modules`, `.next`, and `tsconfig.tsbuildinfo`.

On Windows, the normal tsx CLI exited ENOMEM before project load; schema/pedagogy used an
`os.userInfo`-patched tsx CJS loader. The four shards remain red:

| shard | passed | failed | skipped |
|---|---:|---:|---:|
| 1/4 | 5,651 | 6 | 0 |
| 2/4 | 4,346 | 7 | 0 |
| 3/4 | 2,459 | 5 | 0 |
| 4/4 | 1,461 | 5 | 1 |

The 23 failures cluster in Windows separator assumptions (systems/algebra/avatar fence), Windows
temp-directory EPERM (authz/bad-JSON), a CRLF/path false positive in focus-visible scanning, one
accessible-parity timeout, and installed `nanoid` 3.3.17 below target 3.3.18. Scorer-caused shard-1
excellence failures were reconciled; its focused gate is now **4/4**. Do not call the suite green.

## Browser and deployment boundary

Local production build: homepage showed **129 courses / 1,701 lessons**; Grades 3–5 filtering hid
K and Grade 6 and scoped a fractions search; `absolute-value-piecewise` basecamp showed Course
overview, Start course, optional Recommended prerequisites, and Not started; profile announced
locked badges and showed 15 disabled avatar placeholders with the honest “on the way” caption.

The exact supplied Vercel deployment remained login-protected and the connector could not resolve
the project. Canonical production was reviewed only to reproduce baseline defects. There is no
exact-preview or post-deployment claim.

## Avatar status — PRE-CANARY / NON-SHIPPING

Candidates `001/101/201/301` are independent renders, not board crops. Review rejected inconsistent
framing: `001` silhouette width 59.6% versus 76.4–79.1% for `101/201`; `301` top margin 20.5%
versus 2.5–6.3% for the others. One portrait per human band is also not a premium choice surface.
Keep the allowlist empty until a coherent multi-choice cohort passes contact-sheet review.

## Authored-content blocker and next actions

The 200 CML warnings are waived debt, not V4 completion. Of 161 prediction warnings, 18 are
sequencing cases and 143 have no manipulative step anywhere. Reordering or adding lesson-specific
surfaces changes authored structure; rule 1 requires a narrow owner override. The same rule protects
25 authored MCQ shape leaks.

Next: (1) resolve/isolate every shard failure and rerun green; (2) obtain the authoring decision and,
if granted, work in small hash-proven CML batches; (3) create a coherent avatar cohort; (4)
authenticate and verify an exact Vercel preview; (5) only then continue the wider visual-first,
stem-quality, option-parity, standards, accessibility, and evidence programme.

Do not lose: local build evidence is not deployment proof; same concept with new numbers is practice,
not contrast/transfer; judge choices may shuffle but ordered numeric/spatial choices may not; the
four avatar candidates are evidence, not app assets.
