# SESSION 216 EXECUTION REPORT — the first numberLineRay lesson ships at 9.3, and the twelve-engine debt dissolves under individual adjudication

Low-credit session per the standing mandate: Fable did the searching and adjudication directly
(no search agents), one implementor at a time, one combined gate chain. Stop-rule honoured: the
seven remaining engine gaps and all larger architecture deferred.

## 1. P0 — the first numberLineRay lesson (ACCEPT, mathematics 10/10, overall 9.3)

**Fable's adjudication (no worker spent):** targeted greps found `tse-04-02` — "The Sign-Flip
Rule", a lesson entirely about reversal whose own c1 concept card narrates the exact intermediate
state `−2x > −8 → x < 4`. Five gates passed with REACH verified against the widget source
(authored `transforms` buttons, learner-owned relation symbol, solution-set grading). Mastery
score 14/16 → P0. The 2D `inequalities-and-regions` course and every two-step drill in the family
were refused (the engine's state is `a·x REL c`; it cannot hold an additive term).

**The implementor refused the packet — correctly.** Its own integrity gate rejected the step
("begins solved": the picture always IS the solution set, so `−2x > −8` already draws x < 4), and
worse, the untouched start would GRADE CORRECT — a learner could score without ever performing
the flip. Its alternatives table showed every shape that keeps c1's numbers fails worse (one
literally rewards the misconception). It proposed the minimal fix and stopped, touching nothing.

**The fix (`requireSolvedForm`, additive):** correct iff solution set matches AND coefficient is
1; the begins-solved guard now compares set AND form; a new guard requires the target itself be
solved-form. This surfaced a THIRD diagnosis the field makes reachable — right set, wrong form
(e.g. two presses of ÷(−2) → `(−1/2)x > −2`) — which previously would have fallen into a FALSE
inclusivity diagnosis. Its message quotes the learner's own relation and never names the target.

**The step (`tse-04-02/i1b`):** start `−2x > −8`, transforms ÷(−2) and ×(−2) (exact inverses so
the coefficient ladder is reversible; a ×(1/2) distractor was declined as creating a dead end),
target x < 4, predict block aimed at the invariant (the boundary stays at 4; the SET flips).

**Fresh Fable-QA: ACCEPT — mathematics 10, mastery 9, overall 9.3.** It re-derived everything
with its own BigInt oracle, swept 268 reachable states (exactly ONE grades correct), imported the
**S215 evaluator out of the seal tarball** and compared 4,872 states to verify the old path
reduces literally, and proved the reflection is *unavoidable* in both solve orders (both factors
negative + coefficient 1 needs an odd number of presses, so the first press always breaks the
set). It corrected three implementor claims (mutation flips 8 states not 3; the ×(1/2) "dead end"
justification is false though the exclusion is still right; one unreachable-state caveat on
"byte-identical") and flagged one authoring hazard for the future: nothing checks that
coefficient 1 is REACHABLE from the offered transforms. Its batch ruling: **no other step in this
lesson family converts** (all two-step; the four checks carry the lesson's variant-backed mastery
measurement); the best next instance is `tse-04-01`'s positive-coefficient contrast, where the
ray *doesn't* move — isolating the negative as the cause.

## 2. P1 — the twelve-engine debt: dissolved by adjudication, not by decree

Fable adjudicated all twelve individually (read-only greps + the rubric). The result:

- **Three are FALSE POSITIVES of the contract's own regex.** `/"[a-z]+-ghost"/` cannot match a
  multi-segment testid. `trialProbabilityLab` (`tpl-reveal-ghost`), `distributionCompareLab`
  (`dcl-reveal-ghost`) and `graphStoryLab` (`graph-story-ghost`) had house-grammar reveal ghosts
  all along. **The instrument irony is now recorded in the contract's own comment:** the same
  session that fixed a whitespace-blind regex shipped a hyphen-blind one — and two independent
  counts agreed on "12" because both instruments shared the blindness. Matching counts from two
  instruments are not confirmation if the instruments share an assumption.
- **Eight hold err 3 via the rubric's SECOND mechanism** — live model-computed direction cues
  (`MULTI_CONTROL` in processEvents.ts) — which the contract never checked. The rubric's own text
  names both mechanisms and calls generic-copy cues "the weakest form of this level and the most
  contestable"; the contract now tests `ghost OR MULTI_CONTROL membership` (imported from the
  real set, not copied) with that caveat cited.
- **One was genuinely wrong: `covariationScrubber`.** No ghost, no live cues, only authored
  low/high strings — mechanism parity with `numeric`, which holds err 1. Row corrected 3 → 1
  (Σ21 → 19, grade A → B, honestly).

The debt list is now **empty**, with its structure and both guard directions retained (a 13th
engine, or a stale entry, still fails by name — adversarially re-verified on the new predicate).

## 3. P1 — remaining flagship closures

- **`numberLineRay` mobile 2 → 3** (Fable adjudication): both level-3 clauses hold in source
  (every control 44px in both dimensions, class-based; discrete stepper/keyboard alternatives
  beside every fine drag); the S215 adjudicator's sole stated blocker (the failing 44px gate) was
  removed the same session; the rubric's own barBuilder precedent moved 2 → 3 on exactly this
  basis. Σ19 → 20.
- **One vocabulary for systemsExplore:** the lesson band says slope/intercept in every step of
  `se-01-03` except the one the surface itself authored — the surface was the outlier. Controls
  now lead with the lesson's word and bridge once ("slope (the rate)"), narration already spoke
  the equation itself.
- **algebraTiles SVG accessible name** (QA C-1, two sessions carried): now includes the x² pile
  and the rectangle's factor form and fill state, all from model views; classic mats pinned
  byte-identical. The implementor corrected two of its own guesses by reading rendered output
  (the (x+3)(x+2) rectangle has 12 cells, not 4).

## 4. Validation (one serialized chain)

typecheck 0 · vitest **321 files / 12,896 tests, 0 failures** · validate:content 1,840/1,840 ·
lint:pedagogy 1,711/1,711 · registrations clean · engine-registration 127/127 · content proof
**813/813** · hash proof 1,701/1,701 · build 0 · Playwright 115/115 (Trap-D protocol) ·
fresh-extraction reprove at seal.

## 5. Metrics

| metric | value |
|---|---|
| **Lessons gaining their first causal interaction** | **1** (`tse-04-02` — and it is the flip lesson itself) |
| New engine's first authored user | numberLineRay, at QA 9.3 / math 10 |
| Debts resolved | 12 of 12 (3 false-positive, 8 mechanism-2, 1 row corrected) — list now empty |
| Rating changes | 2 (covariationScrubber err 3→1; numberLineRay mobile 2→3), both rubric-cited |
| Refusals honoured | the implementor's packet refusal; six drill conversions refused by QA; the 2D course refused by Fable |
| Content changes | 1 file, 1 inserted step |
| New engines started | 0 (deferred per mandate) |

## 6. Open at close (for S217)

1. `tse-04-01` positive-coefficient contrast step (QA-nominated; the ray that *doesn't* move).
2. The reachability guard QA flagged: nothing checks coefficient 1 is reachable from a spec's
   offered transforms — close before the second `requireSolvedForm` item.
3. O2's disclosure at seal: the "classic byte-identical" pins check testids and control counts,
   not the SVG accessible name — the guarantee is narrower than its name; decide whether to
   widen the pins or rename the guarantee.
4. distributionCompareLab (10 lessons) is the highest-reuse engine whose err mechanism is only
   the ghost — a genuine misconception-contrast model there would be the next quality lift.
5. The seven deferred engine gaps, when quota allows.
