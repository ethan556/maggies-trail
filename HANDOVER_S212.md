# HANDOVER → Session 212

Read `SESSION211_EXECUTION_REPORT.md`, `SESSION211_CONTENT_CHANGE_LEDGER.md`, and
"# S211 REVIEW" in `SESSION208_INTEGRATION_REVIEW.md`.

## 0. State

MMIP v1: four engines on assembled `CanonicalModel` seams; one shared morph/history
implementation (`widgetMorph.ts`, invariant-checked two-path history). RSG family:
`LineCanonical`, `TriangleCanonical`, `LinePairCanonical` (now consumed by
affineRelationshipLab's intersection and systemsExplore's verdict surface). Content baseline:
`SESSION210_LESSON_HASHES.json` (regenerated in S211 for one authorized variant-key addition;
proof 809/809). vec-05-03/k1 re-askable via `reflect-compose@composeMatrix`. algebraTiles area
schema fields landed but INERT (exactly one reader: their own test). Gates at S211 seal:
305 files / 12,424 vitest · Playwright 115/115 · hash 1,701/1,701 · build 0.

## 1. Restart priorities

1. **algebraTiles Part B completion** — the inert fields must gain readers or be removed (the
   reviewer's explicit ultimatum; do not let the seam age). O1's recorded order, each step
   independently releasable: model (x²/frame state, distribute→branch and factor→gather emission,
   decomposition invariant extended to partial-product placements per `algebraTilesPartials`) →
   `evaluate.ts` additive grading gated entirely behind `spec.area !== undefined` (old path
   reduces literally; 27-spec regression pin) → widget rendering + gallery sample (check
   `allSamples.operability` before adding) → docs §7 note.
2. **systemsExplore editable-line schema slice** (per-line ranges/steps/starts, additive) — turns
   the systems surface genuinely breakable (unique→parallel→coincident by learner action);
   wiring is already built and proved at canonical level.
3. **RSG doc split** (approved): `RSG_DESIGN.md` = stable contract; `RSG_DECISIONS.md` =
   append-only dated decisions.
4. Optional, reviewer-suggested: compile-time `useKeyedMorphHistory`/`useExternalMorphHistory`
   split to retire the runtime latch.
5. Then the mandate queue: geometry studio flagships, remaining P1 families, eight engine gaps,
   DEMAND-gated rich-mix clusters (vectors/similarity), world parity (P3).

## 2. Traps (cumulative — read S211 §3 for the working Trap-D protocol)

A: Playwright reuses the 3100 prod server. B: variants.test.ts + content.widgets.audit.test.ts
solo. C: no backgrounded monolithic vitest; foreground directory batches, dot reporter.
D (working protocol): `fuser 3100/tcp` to find/kill the real listener (pgrep lies here);
require `Ready` + zero `EADDRINUSE` in the NEW server's own log before Playwright; a green curl
proves nothing. E: byte-identical mtime churn — hash against the sealed tarball before crying
drift (S211 cleared four such files in one sweep).
Co-work: single-writer locks, sequential widgets.tsx windows, ONE serialized gate chain, the
independent adversarial reviewer before every seal (S211 catches: an unchecked two-path API, a
wrong recorded rationale, `undefined`-into-learner-feedback assertions).

## 3. Tripwires

- `mmipTypes.ts` FROZEN (four consecutive seals byte-verified).
- Hash target `SESSION210_LESSON_HASHES.json` everywhere; the AUTHORIZED map is path-keyed and
  cumulative from the fixed S151 baseline.
- `widgetMorph.useMorphHistory`: keyed vs external paths latch per instance and mixing throws —
  pick one per widget; `recordAs("coalesce")` on an empty stack throws by design.
- Persisted-value tripwires: `leCanonicalFor`, SlopeTriangleW, and algebraTiles' sanctioned
  `{x, c, mat}` duality (grader contract).
- keyboardParityCheck: native `disabled` only; all-exempt throws.
- Variant work follows CLAUDE.md to the letter; `lookupOrThrow` for table lookups that feed
  learner-visible strings — never `!`.
- Rating lifts: S205M rubric only. `describeState` narration: standing product decision.

## 4. Verification chain

As `HANDOVER_S211.md` §4 with Trap D's fuser protocol. Expect: vitest ~12.4k, proof 809/809,
hash 1,701/1,701 vs SESSION210 manifest.
