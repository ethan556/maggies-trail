# Session 150 exact-fit rerank

- Initial reviewed queue: **4**
- Selected closure: **2 lessons / 13 authored experiences**
- Engine: **`pointSetReasoningLab`**

## Accepted closure

- `cg-01-03 — Graphs That Tell Stories`: six main experiences plus one remedial route.
- `dd-04-01 — Range: How Far Data Stretches`: five main experiences plus one remedial route.

The common mathematical object is a finite labelled observation set. Coordinate stories use ordered `(x, y)` observations; range uses one-dimensional values whose endpoints determine spread. The shared engine derives each answer without pretending that path order and distribution range are the same learner action.

## Preserved intentional assessments

- `kc-02-03 — Order the Numbers`
- `dd-01-01 — Questions That Expect Variety`

Both already assess the intended classification/ordering action directly. Converting them merely to reduce the queue would inflate the rubric without stronger causal learning.

## Rejected alternatives

- Extending only `graphStoryLab` would not truthfully cover unordered range data.
- A generic data calculator would hide coordinate semantics, path order, and endpoint structure.
- A four-lesson mega-engine would force the two assessment-first lessons into unrelated manipulation.

## Acceptance contract

- one pure truth state drives rendering, grading, feedback, narration, reveal, accessibility, and variants;
- path length preserves authored point order;
- range is exactly `max − min`; interior values do not alter it;
- added outliers recompute the endpoints;
- fabricated exploration state cannot unlock checking;
- authored prompts, answers, diagnostic feedback, remedials, and variant declarations remain intact.
