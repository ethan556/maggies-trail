# HANDOVER → Session 218

Read `SESSION217_EXECUTION_REPORT.md`, `SESSION217_FABLE_QA.md`,
`SESSION217_CONTENT_CHANGE_LEDGER.md`. Rubric: `docs/CAPABILITY_AXES.md`.

## 0. State

The reversal arc is COMPLETE AS A PAIR: `tse-04-01/i1b` (positive divide — nothing moves, QA
9.25) and `tse-04-02/i1b` (negative divide — the ray swings, QA 9.3), both on `numberLineRay`
with `requireSolvedForm`, both with revert-proofs against their seals. Five lessons now carry
program-authored causal interactions. The reachability guard closes the last known
`requireSolvedForm` authoring hazard (two-message refusal: proved-unreachable vs
could-not-confirm — never assert what a bounded search did not establish). Content proof
**814/814**; hash 1,701/1,701 vs SESSION210 manifest. Gates at S217 seal: 321 files / 12,910
vitest · engine-registration 127/127 · Playwright 115/115 · build 0.

## 1. Queue (priority order, from the S217 stop-rule)

1. **distributionCompareLab misconception-contrast model** — 10 lessons, the highest-reuse
   quality lift available. Its err mechanism is ghost-only today; a genuine wrong-state contrast
   is the ask. Medium cost: plan one implementor window with a fresh QA.
2. **Build-a-set numberLineRay steps** for `ee-05-02` "Graphing Inequalities" (gallery mode, no
   new fields). Apply the five gates FRESH — the lesson has a `numberLinePlace` step whose
   NOVELTY overlap must be adjudicated honestly, not assumed away.
3. **Second algebraTiles distribute lesson** (a(x+b) shape only; factor mode remains display-only
   pending a non-trivial start design — that design is the real task if attempted).
4. The seven deferred engine gaps (nested rules dr-04-02, u-substitution in-05-02, error
   propagation dc-03-02, growth race dc-04-02, movable interval, odometer, quotient mode).
5. Low-urgency: describeState coverage 84/127.

## 2. Fable-first, one session in — what held

- Coordinator-as-implementor works IF the independent QA stays non-negotiable: it found two
  required fixes and a false coordinator claim ("no other test touched") in a session where the
  planner and implementor were the same party. Keep the QA fresh and keep giving it the seal
  tarball.
- The coordinator was caught by its own gates three times (a transcription missing the flat-line
  case — the dual-route design working; an inclusion-shaped pin weaker than its comment; the
  repo's own no-require lint at build). Cheap catches, all pre-seal. The system is doing this,
  not luck.
- Zero escalations off Fable; credits untouched.

## 3. Traps (A–H stand) + two S217 additions

- **I: a bounded search must not speak like an unbounded one.** If a verifier halts on caps, its
  refusal must say "could not confirm", never "proved impossible". Both wordings are pinned.
- **J: `.includes` is not a pin.** `"y = x + 0".includes("y = x")` passes. Anchor string pins to
  delimiters or assert equality; a pin whose comment claims more than its assertion checks is the
  S215 scope-narrower-than-claim defect in miniature.

## 4. Tripwires

`mmipTypes.ts` FROZEN. Content proof expects **814**. Engine registration 127. Debt list EMPTY
(both directions armed). `requireSolvedForm` items: target must be solved-form; coefficient 1
must be reachable (guard enforces); keep transform ladders exact-inverse pairs. The classic-spec
pins now include SVG accessible names — a name change is a deliberate edit. `console.error` trap
opt-in, empty allowlist. Rating changes: rubric-cited, non-author adjudication only.

## 5. Verification chain

Unchanged (Trap-D fuser protocol). Expect vitest ~12.9k, proof **814/814**, hash 1,701/1,701,
engine registration 127/127.
