# HANDOVER → Session 219

Read `SESSION218_EXECUTION_REPORT.md`, `SESSION218_FABLE_QA.md` (including its delta section),
`SESSION218_CONTENT_CHANGE_LEDGER.md`.

## 0. State

Six lessons carry program-authored causal interactions. `numberLineRay` serves 3 lessons and its
build tasks are RE-ASKABLE (`g7-tse-inequality-build@graphBuild`). distributionCompareLab's judge
mode has a responding picture on 7 authored steps (zero content bytes). Content proof **815/815**;
hash 1,701/1,701 vs the SESSION210 manifest. Gates at seal: 322 files / **12,925** vitest ·
engine-registration 127/127 · Playwright 115/115 · build 0. Fable-only for two consecutive
sessions; zero escalations; credits untouched.

## 1. Queue (priority order)

1. **The manipulable wrong-state contrast for distributionCompareLab** — the queue item's other
   half. The evidence overlay now ANSWERS a wrong conclusion; the manipulable version would let
   the learner DRAG the distributions until their claimed conclusion becomes true and read off
   what gap it requires. Needs a design decision first: does dragging fit a judge step's demand,
   or does it belong in a new explore mode so the judge claim stays supplied by the learner?
   Decide before building; a drag that answers the question for them fails DEMAND.
2. **Overlap-label placement** (collides with its own bracket at some gaps — QA polish note).
3. **Second algebraTiles distribute lesson** (a(x+b) only; factor mode still display-only).
4. **The seven engine gaps** (nested rules dr-04-02, u-substitution in-05-02, error propagation
   dc-03-02, growth race dc-04-02, movable interval, odometer, quotient mode).
5. describeState coverage 84/127 (low urgency).

## 2. What S218 adds to the pattern book

- **Print-and-read is not optional and catches what gates cannot.** Two defects shipped past a
  green 150-seed gate and were caught only by reading four printed items: "A open circle"
  (morphology) and an in-set "test number" that pinned nothing. Both are now gate assertions —
  but only because a human-style read found them first. CLAUDE.md step 5 stands.
- **Leak rules are task-relative.** The solve-task rule (never quote the answer) bans the wrong
  thing in a build task, where the inequality is the printed GIVEN and the answer is the DRAWING.
  When porting a gate assertion across task types, re-derive what the answer channel IS. The
  corrected rule was validated empirically (the draft would have flagged 125/125 legitimate
  specs).
- **A `-t` filter can verify nothing.** With loop-generated test names, a green scoped run proves
  nothing until the filter is shown to match. QA's own confession, recorded in its report.
- **Converting a step with a variant key converts TWO things.** The widget and the generator form
  must move together, or the resolver invariant goes red (it did — the gate caught it this time).
  The S211 form-addition precedent is the honest repair; key-removal is the fallback trade.

## 3. Traps (A–J stand; nothing new this session)

## 4. Tripwires

Content proof expects **815**. `graphBuild` items: start differs from target in all three facts;
boundary in-window with margin; fallback's second number OUT-of-set; articles stored with noun
phrases. dCL evidence overlay: the overlap figure is the DOCUMENTED visual proxy exp(−g²/8) — do
not "fix" it to the normal-overlap integral without a product decision; the sealed aria has
spoken the proxy since S131. All prior tripwires stand (`mmipTypes.ts` frozen; debt list empty
and armed; `requireSolvedForm` guards; `console.error` trap opt-in/empty; rubric-cited rating
changes only).

## 5. Verification chain

Unchanged (Trap-D fuser protocol). Expect vitest ~12.9k (variants solo **3,993**), proof
**815/815**, hash 1,701/1,701, engine registration 127/127.
