# S272 — Functions G8 figure-truth repair

`similar-triangles-slope` visibly and accessibly states `rise 1 / run 1 = rise 3 / run 3`. It cannot serve as evidence for the lesson's distinct `(0, 0)` to `(4, 8)` slope example, nor as an exact representation of its general prose. The two bindings in `fg-02-02` are therefore withheld.

The lesson's correct slope reasoning, learner copy, and assessed interactions remain intact. No unrelated diagram or accessibility label is presented as proof of the stated coordinates.

QA: idempotence guard, focused regression, schema, pedagogy, strict CML, TypeScript, lint, and scoped diff checks. Queue-compatible effect: two stale illustration rows refresh-close; independent review remains open.
