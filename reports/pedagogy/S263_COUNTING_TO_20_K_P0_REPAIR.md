# S263 Counting to 20 K P0 Repair

## Scope

This source-local packet audits all 13 `counting-to-20-k` lessons and addresses the five current P0 rows without changing shared runtime, figure registry, schema, queue, review cards, cache, ledgers, or standards evidence. Stable lesson and step IDs, widget types, option IDs, correct answers, and evaluator behavior are preserved.

## P0 source closures

| Queue row | Source repair | Evidence |
| --- | --- | --- |
| `VIS-kc-02-02-c1-number-track` | `kc-02-02/c1` now uses `kc-fewer`. | The body, visible marks, and accessible title all compare 3 mats with 5 cats: 3 is fewer and 5 is greater. |
| `VIS-kc-04-03-rem-kc0403-c-bar-part-whole` | The `kc-decompose` remedial now uses `kc-break-apart`. | Body and figure both show the three decompositions of 6: 5 + 1, 4 + 2, and 3 + 3. The follow-up check transfers the idea to 5 = 2 + 3. |
| `EXCELLENCE-kc-01-01` | The challenge is now an authored conservation prediction rather than another direct counting-generator instance. | Learners reason that moving the same six dots changes spacing but not quantity, without recounting. The `subitizeFlash` evaluator and answer remain unchanged. |
| `EXCELLENCE-kc-04-03` | The challenge now transforms a split while holding the whole invariant. | Learners move one counter from the 5-part to the 2-part and infer 4 + 3 = 7. The MCQ evaluator, option IDs, and correct option `a` remain stable. |
| `PROGRESSION-kc-02-03` | `k3` now has an explicit zero-boundary prompt. | The prompt is no longer a copy of the earlier ordering action: it asks learners to start with the number meaning none while ordering 0, 3, and 8. |

Result: **5/5 P0 rows source-closed; 0 P0 residuals.**

## Additional live truth repair

The whole-course audit found one unqueued learner-visible contradiction in `kc-04-03/c1`: the prose taught 5 split into 2 and 3 beside the fixed `bar-part-whole` diagram for 13 = 6 + 7. The concept now uses `koa-join-two-groups`, whose visible and accessible claims show exactly two blue counters and three orange counters joining to make five. The next concept remains distinct by generalizing to several decompositions of six.

## Executable evidence

- `scripts/audit/repair-counting-to-20-k-s263.mjs` is guarded, preserves evaluator types and stable IDs, leaves nine non-target lessons byte-for-byte unchanged, and is idempotent.
- `src/lib/session263.countingTo20KP0Integrity.test.tsx` audits the complete 13-lesson corpus for schema, pedagogy, widget integrity, figure SSR/accessibility/numeric parity, distinct challenge and progression jobs, and whole-course MCQ evaluator/feedback agreement.
- Focused regression: 6/6 tests pass.
- Full gates: content schema, pedagogy (1,711/1,711 clean), strict CML (0 errors, 0 warnings), TypeScript, and scoped ESLint all pass.
- Current course seal: `7e52b6bc37617b778432fea4c91b957fa36a43ba1f13bc6c7d22363f9f32220a`.

## Boundaries

P1 lesson, visual-disposition, language, and remaining progression rows are not self-closed. They remain subject to independent current-source assessment. No authoritative queue or disposition artifact was regenerated by this packet.
