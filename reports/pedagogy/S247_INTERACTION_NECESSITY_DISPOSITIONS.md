# S247 interaction-necessity dispositions

The final three `INTERACTION_NECESSITY_REVIEW` rows were re-read against their complete lessons
and the actual candidate-engine contracts.

- `dr-04-02` — **REFUSE** `derivativeRuleLab`. Its chain mode coordinates two local rates; this
  lesson asks learners to order three nested layers inside a product. Substituting the lab would
  hide that structure and misrepresent the required rule order. The layer figure, structure MCQ,
  staged product decomposition, and independent numeric check remain.
- `dc-03-02` — **KEEP**. Three `exactNumberLab` tasks already vary absolute error, percentage
  error, and reverse tolerance; a staged derivation connects powers to relative error. A fourth
  interaction would duplicate the relationship. The separate exact-number answer-signalling debt
  remains open and is not waived here.
- `exp-04-03` — **KEEP**. `expLogExplore` already makes base changes causal, while three
  concept figures show curve overtaking and the differences-versus-ratios distinction. A separate
  growth-race engine would duplicate that evidence.

The source audit now contains no `AUDIT-NEXT` or `AUDIT-OPEN` rows. Queue reconciliation should
remove exactly three P1 review rows; no runtime, lesson, evaluator, or schema behavior changed.
