# S272 — Add/Subtract within 20 figure-truth repair

`as-fact-family` visibly and accessibly uses exactly `6`, `7`, and `13`. The concept now names all four matching facts in visible and narrated copy, so the diagram is a direct worked example rather than an unrelated fact family.

QA: idempotence guard, focused regression, schema, pedagogy, strict CML, TypeScript, lint, and scoped diff checks. Queue-compatible effect: one stale illustration row refresh-closes; independent review remains open.
