# S246 Derivatives-in-context freshness packet

Status: PASS for the complete live `g13-derivatives-in-context` freshness lane. This does not close the full V4 programme.

## Scope closed

Eleven consumed forms now generate genuine mathematical variation:

- perpendicular related-rate selection;
- position, velocity, acceleration, rest-count, and rest-position motion jobs;
- speeding-up sign intervals;
- displacement, motion-leg, and total-distance jobs;
- circle and sphere related rates;
- ladder height and downward-rate jobs;
- square-root and cubic linearisation;
- structured exact-number differential/error labs;
- quadratic linearisation-error scaling;
- polynomial L'Hopital limits;
- polynomial/exponential and power/logarithm indeterminate forms.

Each form is deterministic for a fixed seed, varies learner-visible mathematical state across seeds, validates against the widget schema, and has an independent prompt-derived solver. Structured differential work remains an `exactNumberLab`; it was not flattened into a text-only numeric item.

## Evidence

- Focused family suite: 11/11 pass over 160-256 seeds per job.
- TypeScript typecheck: pass.
- Global resolver: all live derivatives-in-context consumers pass; the next independent frontier is `de-01-01/k1` in `g13-differential-equations`.

## Programme status

Standards approval, whole-lesson dispositions, symbolic presentation rows, illustration replacements, progression rows, and choice-surface rows remain open. This packet is a Phase 5 generator-assurance closure only.
