# S246 Function-analysis freshness packet

Status: PASS for all live `g12-function-analysis` resolver consumers. The overall V4 programme remains partial.

## Scope closed

- vertex-form graph evaluation;
- composition order in both directions;
- square-root domain floors and caps;
- inner/outer function decomposition across powers 2-4;
- absolute-value one-to-one collisions;
- restricted quadratic inverse branches;
- inverse-graph coordinate reflection;
- affine inverse round-trip verification.

Structured questions remain `exactNumberLab` or `pointEntry` surfaces. Every changed family validates against the widget schema, varies learner-visible mathematical state, replays deterministically for a fixed seed, and is independently solved from the rendered prompt.

## Evidence

- Focused assurance: 8/8 tests over 160-192 seeds per form.
- Full `g12-function-analysis` variant family: 23/23 pass.
- TypeScript typecheck: pass.
- Targeted lint: no errors.
- Global resolver: all function-analysis consumers pass; the next independent frontier is `gf-03-01/k1` in `g10-geometry-foundations`.

This packet does not close standards, lesson dispositions, symbolic presentation, illustration, progression, or choice-surface queues.
