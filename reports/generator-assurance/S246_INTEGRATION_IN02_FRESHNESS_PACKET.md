# S246 integration `in-02` freshness packet

## Scope and boundary

- Generator: `g13-integration-accumulation`
- Lessons: `in-02-01`, `in-02-02`, and `in-02-03`
- Consumers: 10 declared steps across 8 distinct forms
- Entry frontier: `in-02-01.json/k1` -> `integration-accumulation__in-accumulation__numeric`
- Stop frontier: `in-03-01.json/ch1` -> `integration-accumulation__in-ftc1__numeric`

No lesson JSON, standard, visual, queue, card, cache, or global generated-evidence file changed in this packet.

## Root cause

All eight `in-02` forms fell back to authored template banks containing only 1-3 prompts per form. The entry-frontier resolver sample produced only 2 distinct widgets, below the freshness gate of more than 3. Repeated consumers therefore changed wording or option order too little, and most forms did not vary their mathematical truth at all.

## Implementation

The integration generator now dispatches all eight `in-02` forms to bounded dynamic builders:

| Form | Preserved mathematical job | Dynamic prompt pool | Dynamic truth pool |
| --- | --- | ---: | ---: |
| `in-accumulation__numeric` | Evaluate an accumulation value or an accumulation difference | 12 | 11 |
| `in-accumulation__mcq` | Identify the dummy variable of integration | 12 | 11 |
| `in-read-accumulation__mcq` | Locate an extremum of the accumulation from the sign of `f` | 12 | 6 |
| `in-read-accumulation__matchPairs` | Connect `f(x)` with the instantaneous behavior of `A` | 12 | 12 |
| `in-signed-area__numeric` | Combine above-axis and below-axis regions as signed area | 12 | 12 |
| `in-read-accumulation__numeric` | Count turning points from sign-changing zeros of `f` | 12 | 6 |
| `in-net-change__mcq` | Interpret accumulated units or distinguish displacement from total travel | 12 | 12 |
| `in-net-change__numeric` | Integrate a signed linear rate in context | 12 | 10 |

Total designed prompt pool increased from 12 authored prompts across the family to 96 dynamic prompts. The designed truth pool increased from 12 authored answers to 80 prompt-derived truths. Numeric distractors target final-value/rate confusion, omitted triangle factors, unsigned-area reasoning, endpoint-only reasoning, and accumulation addition/subtraction errors. MCQ options are shuffled after truth is established.

## Independent truth contract

`calculusIndependent.cjs` independently parses every learner-visible prompt and recomputes the answer. It does not read generated answer fields or MCQ correctness markers. For matching, prompt-derived pairs are recomputed first; the presented columns are used only to verify that the derived labels are present.

The focused test samples 240 generator seeds and 96 unseen resolver seeds per form. It checks exact replay, schema validity, exact prompt/truth pool coverage, one defensible MCQ answer, stable answer-ID agreement, distinct numeric errors, non-positional matching, and independent prompt-derived truth.

## Evidence

- Focused packet + predecessor packet: PASS, 4/4 tests.
- Focused packet alone after exact pool ratchets: PASS, 2/2 tests.
- Typecheck: PASS.
- Targeted ESLint: PASS with 0 errors; 3 pre-existing `no-explicit-any` warnings in `calculusVariants.ts`.
- Full resolver: all `in-02` consumers pass; the suite stops at the next untouched frontier, `in-03-01.json/ch1` / `integration-accumulation__in-ftc1__numeric`, with only 1 distinct widget. That nonzero suite result is expected and was not expanded into this packet.

Global freshness evidence must be regenerated by the coordinating lane after all generator packets finish.
