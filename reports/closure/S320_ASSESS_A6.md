# S320 Independent Course Assessment — Lane A6

Reviewer: Claude Cowork independent assessor (S320)
Reviewed at: 2026-08-20T18:33:47.000Z
Authority: `reports/cache/CHATGPT_WORK_V4_EXACT_PREFIX.md` (repository source + human-decision ledgers are authoritative; the ChatGPT Work cache and prior evidence-only assessments are derived accelerators only, not binding).
Scope: THREE complete courses, course.json + every lesson in full, READ-ONLY on content. Dispositions written to `reports/closure/cowork-staging/laneB-s320-A6-dispositions.jsonl` (36 lesson-disposition records, one per lesson, NDJSON). This file is the companion rationale + implementation-contract report. The ledger (`reports/closure/LESSON_REVIEW_DECISIONS_S244.jsonl`) was not written.

Method: every place-value claim (through millions), every fraction×whole and fraction×fraction product, and every multi-digit sum/difference was recomputed by hand, digit column by digit column, independent of the authored `answer`/`successFeedback` fields. Duplication was scanned programmatically two ways beyond the repo's built-in exact-match `buildDuplicateInventory` (which returned zero hits for this lane): (1) grouping MCQs by sorted option-label-set + sorted feedback-set regardless of prompt wording, and (2) grouping numeric checks by prompt-core-after-stripping-generic-framing-phrases + answer. MCQ/predict option order in source JSON was **not** treated as a defect signal (confirmed seeded-shuffle-at-render, graded by option id not array index, per `S316_LAB_CHOICE_SHUFFLE_SWEEP.md`); only genuine label-length outliers (correct option uniquely longest/shortest, gap not attributable to legitimate added mathematical content) were treated as option-parity risks.

## Per-course totals

| Course | Lessons | KEEP | REVISE | ESCALATE |
|---|---|---|---|---|
| place-value-million | 14 | 9 | 5 | 0 |
| fraction-multiply-g4 | 12 | 9 | 3 | 0 |
| add-subtract-1000-g3 | 10 | 6 | 4 | 0 |
| **Total** | **36** | **24** | **12** | **0** |

---

## place-value-million (14 lessons — 9 KEEP, 5 REVISE)

course.json: clean (5 chapters, 14 lessons, all lessonIds resolve, no orphans).

- **pv2-01-01** — REVISE. `k3`'s second `explanationVariants` entry contains a visible, unedited internal aside: *"One rung up is ×10 (100 hundred-thousands... wait, one rung is 10 hundred-thousands per million); two rungs from ten-thousands is 10 × 10 = 100."* The final math (100) is correct, but the "...wait..." self-correction is a raw editing artifact that should never reach a 4th grader. **Implementation contract:** replace that explanationVariant with a clean sentence, e.g. "One rung up is ×10; two rungs from ten-thousands to millions is 10 × 10 = 100." No numeric change needed.
- **pv2-01-02** — KEEP. All ×10/÷10 shift claims recompute correct (507→5,070; 4,800→480; 630→6,300; 25→250→2,500).
- **pv2-01-03** — KEEP. All place-value/expanded-form claims recompute correct (425,301 digit 5 = 5,000; 320,005; 704,060; 719,000 digit 7 = 700,000; 503,082).
- **pv2-02-01** — REVISE. `k1`'s commonError feedback for `640012` claims it "swaps the hundred-thousands and ten-thousands digits," but digit-by-digit comparison of 604,012 vs. 640,012 shows the hundred-thousands digit (6) is identical in both — it is the **ten-thousands and thousands** digits (0↔4) that are actually swapped. Separately, `i2`'s `commonBuilds` feedback for the wrong `30,000` token ("reads the 2 as if it belonged to the thousands column") is not internally coherent — a 2 read as thousands would be worth 2,000, not 30,000; the actual source of 30,000 is misreading the ten-thousands digit 2 as if it were a 3. **Implementation contract:** in `k1.widget.commonErrors`, change the `640012` feedback to name the ten-thousands/thousands swap instead of hundred-thousands/ten-thousands; in `i2.widget.commonBuilds[0]`, rewrite the feedback to correctly attribute `30,000` to misreading the ten-thousands digit (2) as a 3, not to "the thousands column."
- **pv2-02-02** — KEEP. All silent-zero reading claims recompute correct (500,009; 40,020; 703,040; 200,005).
- **pv2-02-03** — KEEP. All comma-grouping claims recompute correct (9,040; 618,204; 63,500/7,200/142,000; 620,345; 506,732 → 1 comma).
- **pv2-03-01** — KEEP. All rounding claims recompute correct through the million rollover (428,619→429,000/430,000; 652,340→650,000; 345,500 halfway→346,000; 999,600→1,000,000).
- **pv2-03-02** — KEEP. Round-then-add vs. add-then-round drift examples all recompute correct (62,300+24,800; 38,600+45,900; 24,600+36,800), including the subtle "both pieces round up individually yet the exact total rounds down" cases.
- **pv2-03-03** — KEEP. All front-end-truncation and agree/disagree-with-true-rounding comparisons recompute correct (53,720; 68,412+21,955; 47,300+52,600; 38,600+24,900; all four dragBucket items).
- **pv2-04-01** — REVISE. `k2`'s `body` field reads "A three-trade cascade," but the worked problem (156,489 + 267,742) hand-verifies to **five** cascading trades (ones→tens→hundreds→thousands→ten-thousands), exactly as the same step's own `explanationVariants` and `successFeedback` already correctly state ("five trades in a row... 424,231"). The `body` field contradicts the rest of its own step. **Implementation contract:** change `k2.body` from "A three-trade cascade." to "A five-trade cascade." (or equivalent), matching the verified explanationVariants/successFeedback.
- **pv2-04-02** — REVISE. `k1`'s `body` ("One break.") and second `explanationVariants` entry ("Only the ones column needs a break here.") both undercount 6,412 − 1,847's true break cascade: hand-tracing the borrow shows ones, tens, AND hundreds all break (2<7 forces a break that cascades through tens, which itself is then short against 4), matching that same step's own correct `successFeedback` ("three borrows, each one broken by hand"). Separately, `k2`'s first `explanationVariants` entry undercounts 83,251 − 46,378 as needing breaks in "the ones, tens, and hundreds columns"; hand-tracing shows **four** columns break (ones, tens, hundreds, AND thousands — the thousands digit 3 is short against 6 after hundreds' break leaves it needing to feed hundreds too), yet the final answer 36,873 itself is correct. **Implementation contract:** change `k1.body` to "Three breaks." and rewrite the false explanationVariant to state three columns break (ones, tens, hundreds); rewrite `k2`'s explanationVariant to list ones, tens, hundreds, and thousands.
- **pv2-04-03** — REVISE. `i2`'s `matchPairs` widget (matching minuends to how many zeros their borrow chain passes) has correct pairings, but its `pairErrors` feedback and `successFeedback` text for the 40,052 item are factually confused: they say "the hundreds digit is already 5 (nonzero)" and "reaching hundreds from tens... 5 is already there," but 40,052's hundreds digit is actually **0** — the digit that is 5 is the **tens** digit, and the correct direction of the chain is ones-borrowing-from-tens (not "hundreds from tens"). **Implementation contract:** rewrite `i2.widget.pairErrors[0].feedback` and `i2.widget.successFeedback` to correctly state that the ones column's borrow resolves immediately at the nonzero tens digit (5), so 0 zeros are passed — not referencing "the hundreds digit" at all for this item.
- **pv2-05-01** — KEEP. All digit-count and place-by-place comparison claims recompute correct (999,999 vs. 1,000,000; 428,619 vs. 425,983; all three `matchPairs` items; 340,872 − 89,999 = 250,873 → 250,000).
- **pv2-05-02** — KEEP. All ordering/ranking claims recompute correct, including the five-town tie-break chain (Cedar Falls < Dunmore < Ashville < Elmsworth < Bridgeton) and 604,102 − 89,999 = 514,103.

**Pattern note:** 3 of the 5 REVISE items in this course (pv2-04-01, pv2-04-02, pv2-04-03) cluster in the addition/subtraction-with-regrouping chapter and share a common shape — a short summary field (`body`, `explanationVariants`, or `pairErrors`/`successFeedback`) miscounts or mislabels which/how-many columns actually cascade, even though the underlying numeric answer is correct and (in two of three cases) the SAME step's own other prose already states the true count correctly. Worth a targeted regression check on this course's other "cascade summary" fields if the generator/template that produces them is touched.

---

## fraction-multiply-g4 (12 lessons — 9 KEEP, 3 REVISE)

course.json: clean (3 chapters × 4 lessons, all lessonIds resolve). Confirmed current repaired state for both historical flags named in the assignment: the S304 P0 figure fail-close holds (all 8 figure ids this course cites — `fa-add-like`, `fm-groups`, `fa-repeated-add`, `number-line-jumps`, `frac-numline-pastone`, `fa-improper-mixed`, `fa-benchmark-half`, `frac-unit-fourth` — are registered in `figureIds.ts` and implemented in `figures.tsx`; spot-checked `FaImproperMixed`, which renders exactly 11/8 = 1 whole + 3/8, matching its caption); the one remaining "the figure shows..." sentence in the corpus (g4x-02-04 `c1`) is honestly hedged ("shows the same conversion direction with **another value**, 11/8 = 1 3/8"), not a false match claim, so it is not re-flagged. Zero "mixed number" violations on exact whole-number quotients found anywhere in this course (checked every hit of "mixed number" in raw text against its numerator/denominator pair).

- **g4x-01-01** — KEEP. Kept as the canonical (first-in-sequence) instance of a duplicate pair — see g4x-01-03.
- **g4x-01-02** — KEEP. No duplication or arithmetic defects found across any step.
- **g4x-01-03** — REVISE. `k3` ("Retrieve in a new form: 2/5 + 2/5 + 2/5 written as multiplication is…") is a verbatim cross-lesson duplicate of g4x-01-01's `k2` ("Apply without the model: 2/5 + 2/5 + 2/5 written as multiplication is…") — identical options, labels, and feedback; only the generic leading framing phrase differs. **Implementation contract:** replace `k3`'s prompt/options/feedback with a distinct repeated-addition-to-multiplication example (different fraction and/or repeat count, e.g. 3/8 + 3/8 + 3/8 + 3/8 as 4 × 3/8) while preserving the g4x-unit-multiples conceptTag's job and the same misconception set (fraction×fraction confusion, whole+fraction confusion, denominator-scaling confusion).
- **g4x-01-04** — KEEP. No duplication or arithmetic defects found across any step.
- **g4x-02-01** — KEEP. `k1`'s correct option is uniquely longest (39 vs. 15/32/18 chars), but this is legitimate added justification ("...landing on 6/5"), and the nearest distractor (32 chars) is close enough that a bare length heuristic would not reliably win; not an exploitable leak.
- **g4x-02-02** — KEEP. `k1`'s length flag is not exploitable — correct (41 chars) and the top distractor (40 chars) are effectively tied.
- **g4x-02-03** — KEEP. `k1`'s extra length is legitimate content (states actual quantities: "15 fourths... 4 fourths"), not a bare-length tell.
- **g4x-02-04** — KEEP. Clean in isolation; kept as the canonical (first-in-sequence) instance of a duplicate pair — see g4x-03-02.
- **g4x-03-01** — KEEP. No duplication or arithmetic defects found; independently reconfirms the prior evidence-only S258 assessment's KEEP call.
- **g4x-03-02** — REVISE. `ch1` ("Transfer to a final context: Convert 2 3/4 to an improper fraction. What is the new numerator (over 4)?") is a verbatim cross-lesson duplicate of g4x-02-04's `k2` ("Apply without the model: Convert 2 3/4 to an improper fraction...") — same answer (11), identical `commonErrors` (5, 8) and feedback text word-for-word. This duplicate was missed by the prior evidence-only S258 assessment, which listed g4x-03-02 as a clean KEEP alongside g4x-02-04 and g4x-03-01 without cross-checking the two against each other. **Implementation contract:** replace `ch1`'s mixed number (currently 2 3/4) with a different mixed-number-to-improper-fraction conversion not reused elsewhere in the course (e.g. 3 1/5), recomputing its answer and commonErrors accordingly.
- **g4x-03-03** — KEEP. Kept as the canonical (first-in-sequence) instance of a duplicate pair — see g4x-03-04. All other arithmetic in this lesson recomputes correct (2×3/8=6/8; 6×3/8=18/8=2¼; 5×2/8=10/8; 23/8's whole part=2; 4×3/10=12/10).
- **g4x-03-04** — REVISE. `k2` ("Compute 3 × 2/6 as ?/6. What is the numerator?...") is a verbatim cross-lesson duplicate of g4x-03-03's `k2` — identical prompt, answer (6), commonErrors, and feedback. `k3`'s length flag (42 vs. nearest 37 chars) is legitimate content, not a leak. **Implementation contract:** replace `k2`'s whole-number-times-fraction pair (currently 3 × 2/6) with a fresh example not used elsewhere in this lesson or in g4x-03-03 (this lesson already uses 7×5/6, 5×4/5, 4×3/4, 6×4/5 — e.g. 4 × 2/9 = 8/9 would be unused), recomputing answer/commonErrors/feedback accordingly.

---

## add-subtract-1000-g3 (10 lessons — 6 KEEP, 4 REVISE)

course.json: clean (3 chapters, 10 lessons, all lessonIds resolve).

- **g3a-01-01** — KEEP. All sums recompute correct (243+135=378; 312+222=534; 433+443=876; 421+321=742; 581's tens-digit value=80).
- **g3a-01-02** — KEEP. All carry-cascade sums recompute correct (167+148=315; 137+216=353; 286+157=443; 188+377=565; 138+136=274; 267+377=644).
- **g3a-01-03** — KEEP. All no-regroup differences recompute correct (486−254=232; 574−361=213; 754−321=433; 548−106=442; 775−142=633; 879−307=572).
- **g3a-01-04** — KEEP. All double-regroup differences and flip-bug distractors recompute correct (452−178=274; 883−165=718; 631−257=374; 651−402=249; 533−105=428; 882−315=567).
- **g3a-02-01** — KEEP. All borrow-through-zero differences and flip-bug distractors recompute correct (503−267=236; 500−154=346; 704−358=346; 600−173=427; 500−334=166).
- **g3a-02-02** — KEEP. All open-number-line hops recompute correct (347+200=547; 246+300=546; 282+100=382; 657+100=757; 243+142=385).
- **g3a-02-03** — REVISE. `k1` and `k3` are verbatim within-lesson duplicate MCQs: both ask "To add 298 + 145 mentally, Sam adds 300 + 145 = 445 first. What must Sam do next?" with byte-identical options and feedback, differing only in the trailing generic framing phrase ("Work place by place." vs. "Name the deciding place-value fact."). This is a genuine repeat of the same reasoning question as two supposedly-independent checks in the main lesson flow (not a remedial retry, which is a by-design, acceptable pattern used elsewhere and not flagged). **Implementation contract:** replace `k3`'s prompt/options/feedback with a distinct compensation-strategy item — either a new number pair requiring the same "round up then repay" reasoning (e.g. 396 + 258, round to 400) or a numeric follow-up asking for the corrected total instead of repeating the same four-option reasoning menu.
- **g3a-03-01** — REVISE. `k1` and `ch1` are verbatim within-lesson duplicate MCQs: both ask "You compute 632 − 178 = 454. Which check confirms it WITHOUT redoing the subtraction?" with byte-identical options/feedback, differing only in trailing framing phrase. `k3` (781−324=457) is distinct and correct. **Implementation contract:** replace `ch1`'s prompt/options/feedback with a distinct inverse-check scenario using a new subtraction fact (e.g. "You compute 541 − 263 = 278. Which check confirms it WITHOUT redoing the subtraction?"), keeping the same four-option shape (correct add-back; redo-subtraction distractor; grows-past-start distractor; compare-not-check distractor).
- **g3a-03-02** — REVISE. `k2` and `ch1` each carry a second `commonErrors` entry whose feedback claims "A hundred went missing in the carry — check the tens column before you commit," but hand-tracing both underlying problems shows **no column carries at all**: 132 + 400 = 532 (ones 2+0, tens 3+0, hundreds 1+4 — none reach 10) and 272 + 417 = 689 (ones 2+7, tens 7+1, hundreds 2+4 — none reach 10). The feedback names a mechanism (a carry in the tens column) that does not exist in either specific problem instance, even though the numeric claim ("100 short") is arithmetically true. A student told to "check the tens column" for a carry that isn't there could reasonably conclude the feedback itself is wrong. **Implementation contract:** rewrite `k2.widget.commonErrors[1].feedback` and `ch1.widget.commonErrors[1].feedback` to name the actual slip for a no-carry addition (e.g. "the hundreds digit was copied down wrong — recheck 1 + 4 in the hundreds column"), or regenerate these two problems with number pairs that genuinely carry through the tens column so the existing feedback text becomes true.
- **g3a-03-03** — REVISE. `k1` and `k3` are verbatim within-lesson duplicate MCQs: both ask "Which is fastest for 245 + 398, and why?" with byte-identical options/feedback, differing only in trailing framing phrase. **Implementation contract:** replace `k3`'s prompt/options/feedback with a distinct strategy-choice scenario using different numbers — ideally one where a different strategy (not compensation) is fastest, e.g. a case favoring the open number line or the standard algorithm, so the two checks test discrimination between strategies rather than repeating the same judgment.

**Pattern note:** 3 of the 4 REVISE items in this course (g3a-02-03, g3a-03-01, g3a-03-03) share the identical shape — a chapter-3 lesson's `k1` and either `k3` or `ch1` are the exact same MCQ, differing only in a trailing instructional-framing phrase ("Work place by place." / "Name the deciding place-value fact." / "Finish with a fresh situation."). All three affected lessons are in chapter 3 ("Checking, Applying, Choosing"), suggesting the content-generation template for that chapter's later reasoning-menu MCQs sometimes reuses the same underlying scenario across two step slots without varying the numbers. Worth a targeted regression check on chapter 3's MCQ generation if that template is touched.

---

## REVISE list (one-phrase reasons)

1. **pv2-01-01** — stray unedited "wait..." self-correction left in a student-facing explanationVariant.
2. **pv2-02-01** — commonError feedback names the wrong pair of swapped digits (says hundred-thousands/ten-thousands, is actually ten-thousands/thousands).
3. **pv2-04-01** — step's own `body` says "three-trade cascade" but the problem needs five trades (contradicts the step's own correct explanationVariants).
4. **pv2-04-02** — two separate cascade-count undercounts (says "one break"/"only ones" for a 3-break problem; says "3 columns" for a 4-break problem).
5. **pv2-04-03** — matchPairs feedback misidentifies a tens digit as "the hundreds digit" and describes the borrow direction backwards.
6. **g4x-01-03** — check item verbatim cross-lesson duplicate of g4x-01-01's check (same repeated-addition-as-multiplication prompt).
7. **g4x-03-02** — challenge item verbatim cross-lesson duplicate of g4x-02-04's check (same "convert 2 3/4" prompt); missed by prior evidence-only assessment.
8. **g4x-03-04** — check item verbatim cross-lesson duplicate of g4x-03-03's check (same "3 × 2/6" prompt).
9. **g3a-02-03** — two checks in one lesson are the verbatim same compensation-reasoning MCQ.
10. **g3a-03-01** — a check and the challenge in one lesson are the verbatim same inverse-check-reasoning MCQ.
11. **g3a-03-02** — commonError feedback blames "the carry" in two problems that hand-verify to have no carry at all.
12. **g3a-03-03** — two checks in one lesson are the verbatim same strategy-choice-reasoning MCQ.

## Notes on scope decisions

- **mcq/predict/lab option order**: authored JSON order is not a defect signal (confirmed seeded-shuffle-at-render, graded by option id, per `S316_LAB_CHOICE_SHUFFLE_SWEEP.md`); no finding above is based on which option appears first in source.
- **Remedial reusing its own lesson's main-check numbers**: every remedial in both courses that use them (place-value-million, add-subtract-1000-g3) exactly repeats an earlier step's numbers by design (a same-difficulty retry after a concept re-teach) — this is the intended mastery-learning scaffold, not duplication, and is not flagged. Only duplication between two *main-flow* steps (check vs. check, or check vs. challenge) was treated as a defect.
- **Generic reusable figures** (e.g. `Pv4CarryChain` always illustrating 2,758+1,463=4,221 regardless of citing lesson): acceptable and not a "visual promise mismatch" as long as no lesson prose falsely claims the figure shows that lesson's own specific numbers — verified true for every figure cited by all 36 lessons in this lane.
- **CML metadata blocks**: templated boilerplate on nearly every step (kernel/invariants/misconceptions/transferFamily), not graded content — not a focus of defect-hunting, consistent with sibling lanes.
- **Length-parity scan**: an initial raw max/min-ratio pass over-flagged many MCQs; the reported scan instead uses a stricter "uniquely-extreme correct-option length, with the runner-up meaningfully shorter/longer" test. Every survivor of that stricter test (g4x-02-01/k1, g4x-02-02/k1, g4x-02-03/k1, g4x-03-04/k3) was inspected by hand and found to reflect legitimate added mathematical justification rather than a bare, content-free length tell; none are flagged.
