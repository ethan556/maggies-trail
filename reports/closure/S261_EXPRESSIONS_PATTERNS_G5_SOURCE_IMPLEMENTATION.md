# S261 expressions-patterns-g5 source implementation

Baseline: 63 rows (24 P0, 39 P1) across 12 clean lessons.

The 24 illustration rows all pointed to the unrelated fixed `count-on-hops` exemplar. Eleven placements were rebound to already-registered semantic figures whose visible and accessible contracts match the authored concept: `dop-precedence`, `dop-word-expr`, `ee-read-aloud-tree`, `cg-pair-terms`, and `cg-line-up`. The other 13 were fail-closed because every available fixed exemplar introduced unsynchronised numbers or a different learner claim.

Both progression causes were repaired at source. `g5e-01-04/i2` now asks learners to choose a valid operation plan instead of repeating a slider computation. `g5e-03-05/i2` now asks for the structural proof behind the pattern instead of repeating a term-building interaction. The `g5e-03-01/k3` choice surface retains stable IDs, correctness, and diagnostic feedback while using parallel, cue-resistant option labels.

Queue-compatible result: 27 source-controlled rows closed—24 illustration replacements, 2 progression causes, and 1 choice-surface cause. Residual: 36 independent assessor-controlled rows (12 lesson dispositions, 12 visual dispositions, and 12 language reviews). No generic disposition is self-closed. Shared runtime, registry, schema, queue, cards, cache, standards, and ledgers are unchanged.
