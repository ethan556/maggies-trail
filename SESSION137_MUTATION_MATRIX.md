# Session 137 adversarial mutation matrix

| mutation | required detector |
|---|---|
| use drawing radius as real radius | scaled-circle independent target test |
| apply the scale twice / use diameter | exact wrong-choice feedback test |
| use `r²` for circumference | scaled-circle choice derivation |
| use `2r` for area | scaled-circle choice derivation |
| allow two correct circle claims | schema integrity uniqueness gate |
| omit drawing radius or scale from only one side | schema paired-field integrity gate |
| solve `2x = 180` instead of `x + 2x = 180` | angle common-path evaluator test |
| use 90° as the straight-line total | angle common-path evaluator test |
| linear-pair card disagrees with target | angle integrity equation gate |
| angle variant falls back to numeric | support/core/stretch seed sweep |
| accept equality as a triangle | strict `a+b>c` helper test |
| show equality as a green closure | triangle renderer flat-state text/pattern check |
| decide by largest-side position | exact triangle choice feedback test |
| decide by unequal side lengths | exact triangle choice feedback test |
| let learner check before exploring | `requiredMoves` canCheck test |
| triangle variant falls back to MCQ | support/core/stretch seed sweep |
| reveal replaces learner selection | visual reveal-preservation test |
| reduce a native target below 44px | component class assertion / keyboard gate |
| convey status by color alone | shape/text/pattern visual test |
| claim adapt=3 without process events | component toward/away event tests |
| miss any registration surface | generated 112/112 registration contract |
| alter prose, order, answer, or feedback | Session-137 content-change proof |
| change any non-target lesson | 1,129-file hash proof |
